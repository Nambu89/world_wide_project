# feedback: NUNCA copiar fuente AGPL de worldmonitor

**Regla:** worldmonitor es AGPL-3.0. Las fórmulas/ideas/pesos NO son copyrightables; el **código SÍ**. Re-implementa la metodología limpia desde cero. NUNCA pegues líneas de worldmonitor.

**Por qué:** el copyleft AGPL es viral — copiar fuente convierte todo el programa en obra derivada AGPL, y la §13 obliga a abrir el código si se expone por red. Re-implementar mantiene el proyecto libre.

**Cómo aplicar:** para CII (`packages/core/cii`), router LLM (`packages/core/ai`), señales (`packages/core/signals`) → lee docs/código solo como referencia metodológica (vía `codebase-navigator`, que marca el código AGPL) y escribe implementación propia. osiris (MIT) sí se puede copiar. Si te ves pegando líneas de worldmonitor → PARA. Ver [[world-wide-project-goal]] e INVESTIGACION-FUSION.md §4.6.
