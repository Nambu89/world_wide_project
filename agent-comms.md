# agent-comms.md — Canal inter-agente (append-only)

> Formato: `## [ISO-TIMESTAMP] [AGENT] [STATUS] — mensaje`
> Vocabulario de status: `DONE` 🟢 · `IN_PROGRESS` 🟡 · `BLOCKED` 🔴 · `NEEDS_REVIEW` 📢
> Contrato: cada agente LEE al empezar (últimas entradas) y ESCRIBE al terminar.

---

## [2026-06-16T16:30:00Z] [DATA-CONNECTOR-DEV] [DONE] — T-36 DONE: conector sanctions.ts (OFAC SDN list, OpenSanctions CC BY-NC 4.0, keyless). Fuente: data.opensanctions.org/datasets/latest/us_ofac_sdn/targets.simple.csv. Zero-key. Patrón osiris: AbortSignal.timeout(15000) + User-Agent + single-flight + serve-stale (TTL 25h) + retorno vacío gracioso. Parseo CSV quote-aware hand-roll (~35 líneas, zero-dep): comas dentro de campos entrecomillados, doble-comilla escape. W3: índice de columna 'countries' derivado del header (robusto a reordenación). ISO-2 → Intl.DisplayNames + CANONICAL_ALIASES (cd→Congo(Kinshasa), ps→Palestinian Territories, cz→Czech Republic, mm→Myanmar, cg→Congo(Brazzaville)). ISO inválido/Unknown Region → drop gracioso. Retorna ConnectorResult<SanctionRow>. Limitación `// ponytail:`: newlines embebidos en un campo entrecomillado no están soportados (parseo línea-a-línea); muestra del PM no los tenía; si aparecen en producción → NEEDS_CONTEXT. 29/29 tests verdes (6 suites: parseCsvLine, isoToName, aggregateFromCsv, fetchSanctions contrato, fallback, doesNotReject). tsc --noEmit limpio. barrel index.ts actualizado con export fetchSanctions. Ficheros: sanctions.ts (203 líneas), sanctions.test.ts (315 líneas), index.ts (+2 líneas). Self-Report: real.

## [2026-06-16T12:00:00Z] [INTEL-ANALYST] [DONE] — T-38 DONE: bloque sanciones OFAC en briefing (Approach B, espejo exacto de buildConvergenceContext). buildSanctionsContext(latest: SanctionRow[]): top-10 por sanctionedCount desc, '' si vacío. serializeContext gana 5º arg sanctions: SanctionRow[]=[] (default 0 — callers existentes intactos, D-311). generateDailyBriefing lee getLatestSanctions(). buildSanctionsContext re-exportada en index.ts. 56/56 tests verdes (11 suites; suites nuevas: buildSanctionsContext 5 tests + serializeContext-sanciones 6 tests). tsc --noEmit limpio. Ficheros: briefing.ts (+38 líneas), index.ts (+1 línea), ai.test.ts (+132 líneas). Sin proveedor nuevo, sin 2ª llamada LLM, caché D-106 intacta. Self-Report: real.

## [2026-06-16T10:00:00Z] [BACKEND-ARCHITECT] [DONE] — T-33 DONE: GET /api/convergence solo-lectura (getLatestConvergence + centroides, espejo exacto de /api/cii) + "Palestinian Territories" añadido a COUNTRY_CENTROIDS. 57/57 tests verdes (37 previos + 9 nuevos T-33 + 4 regression-guard T-33 + demás). Ficheros: server.ts (+import getLatestConvergence+ConvergenceSignalRow, +ruta /api/convergence antes del 404), server.test.ts (+seed insertConvergenceSignals, +9 tests T-33), packages/connectors/geo/country-centroids.ts (+Palestinian Territories lat:31.9 lon:35.2). Self-Report: real.

## [2026-06-16T00:00:00Z] [SYSTEM-ARCHITECT] [DONE] — design-doc: docs/design/2026-06-16-convergence-surface.md (## PLANNING COMPLETE). Rebanada 5 = superficie UI de convergencia, ESPEJO estricto de la superficie CII (rebanada 3). Construye lo que el motor (rebanada 4, NG-4) dejó fuera: GET /api/convergence (getLatestConvergence + centroides, solo-lectura), CONVERGENCE_LAYERS en config-array (glifo ANILLO/halo por strength, distinto del círculo CII, toggle independiente), ConvergencePanel.tsx (señales por strength + familias + flecha dynamicScore + first-detected), 5ª pestaña Convergencia con map-tie reusando activeCountry. camelCase wire (L-1). NO toca el motor/job/migración (NG-1). 10 decisiones D-400..409 + 2 bloqueadas. PM ratifica: GAP-3/OQ-1 (¿incluir /api/convergence/:country D-407 o diferir — recomiendo diferir), OQ-2 (color del anillo por strength vs topDimension — recomiendo strength), OQ-3 (toggle convergencia apagado por defecto — recomiendo sí), GAP-1 (forma de componentsJson sin inspeccionar campo-a-campo), GAP-2 (cobertura centroides "Palestinian Territories"). ADR a registrar por PM = ADR-013.

## [2026-06-13] [PM] [DONE] — Fase 0 completada: andamiaje `.claude/` (11 agentes) + blackboard sembrado (CLAUDE.md, plans/ROADMAP.md, plans/DECISIONS.md ADR-001..004, memory/MEMORY.md + feedback). Pendiente: fijar alcance del MVP vía `/design` antes de implementar código de producto.

## [2026-06-13] [SYSTEM-ARCHITECT] [DONE] — Design-doc MVP Finanzas creado en `docs/design/2026-06-13-mvp-finanzas.md` (registrado por el PM; el architect no escribe en este canal). Schema time-series libSQL (5 tablas), scheduler 4 tiers, 3 conectores keyless patrón osiris, router rama claude + briefing cacheado 24h, server.ts con pipeline de seguridad, panel Finanzas + config-array de capas. 8 Non-Goals (convergencia §9.1 = spike futuro). 9 decisiones internas D-100..108 con tradeoffs. Self-Report: real (verificado por PM en disco).

## [2026-06-13] [PM] [IN_PROGRESS] — Usuario APRUEBA el plan + añade requisito transversal: UI responsive + mobile-first → registrado ADR-008, cableado en T-07 (constraint + acceptance 375/1200px). Cadencia elegida: WAVE A WAVE con checkpoint. Arrancando Wave 1 = T-01 (bootstrap, backend-architect); valida R-1 (libSQL nativo Windows) antes de seguir. Waves 1-5 no afectadas por ADR-008; re-validación de scope de T-07 antes de Wave 6.

## [2026-06-13] [PLAN-CHECKER] [DONE] — Plan `plans/2026-06-13-mvp-finanzas.md`: **PASS** (0 issues bloqueantes, 4 warnings menores). 7 dimensiones limpias: cobertura (G-1..8 + D-100..108), completitud (verify <60s/tarea), dependencias (grafo acíclico, wave-3 disjunta fichero a fichero), scope (sin creep ni frases prohibidas), riesgos (R-1..8 mitigados), fidelidad D-NN, frases de erosión. PM aplicó las 3 recomendaciones no-bloqueantes (web-api condicional eliminado, daily-orchestration→acceptance T-04, subset .env→T-01). Registrado por el PM (plan-checker es read-only).

## [2026-06-15] [SYSTEM-ARCHITECT] [DONE] — design-doc: docs/design/2026-06-15-cii-scoring.md (## PLANNING COMPLETE). REFRESCO del CII: supersede el data-starved 2026-06-13. Premisa invertida — events (getEventsByCountry, severity/country REALES) + signals (GKG) dan fuente real keyless a 4 componentes {conflict,economic,political,social}. EVENT_BLEND propio {0.25/0.30/0.20/0.25}, COMPOSITE baseline*0.4+event*0.6, FLOORS, decay exp 30d, boosts EQ/fire vivos. Migración corregida a 004_cii.sql, tier→medium. Mantiene D-001..005, motor N-componentes signalPresent+renorm, cii_snapshots, /api/cii. Mueren R-2/NG-5 (sesgo país-fuente, reverse-geocode). 6 OQ a ratificar PM (mapeo sub-señal→fuente, clave país, componentes presentes, floors/decay, tier, geometría capa). Schema auto-validado: front-matter OK, 10 secciones en orden, 7 Non-Goals, IDs D-001..005+D-200..213 únicos, sin token colgante. Self-Report: real (verificado en disco).

## [2026-06-13] [PM] [DONE] — MVP scope LOCKED. Interrogación resuelta (3 conflictos de la propuesta): IA=Anthropic Claude rama activa del router (ADR-005), persistencia=@libsql/client `file://` local Turso-compatible (ADR-006), entorno=pnpm workspace + `.venv` Python reservado (ADR-007). ROADMAP Fase 1 actualizado. Siguiente: dispatch `system-architect` → `docs/design/2026-06-13-mvp-finanzas.md` (RPI Research+Design), luego plan → `/check-plan` (gate PREVIO).

## [2026-06-13] [PM] [IN_PROGRESS] — Waves 2+3 lanzadas en **workflow paralelo** (`wf_a9cf2219-1ff`) a petición del usuario (paralelizar sin pisarse). Seguridad: lock por disjunción de ficheros; secuencial entre waves; cada agente LEE los contratos reales en disco de la wave previa (no reinventa). Wave 2 = T-02 store (impl backend-architect → verify adversarial verifier → **early-abort si no VERIFIED**). Wave 3 (solo si store OK) = 4 agentes EN PARALELO con ficheros disjuntos: T-03a finance/markets · T-03b geo/gdelt · T-03c edu/news (data-connector-dev, write-only para evitar carrera del tsc de paquete compartido) · T-05 core/ai (intel-analyst, auto-verifica su propio paquete) → barrera → verify consolidado del paquete connectors + core/ai. Waves 4→6 (scheduler→server→web) son secuenciales (dependencia dura), van después.

## [2026-06-13] [PM] [DONE] — **Waves 2+3 COMPLETADAS y VERIFICADAS** (verify propio del PM, no relay). Wave 2 = T-02 store (backend-architect): build OK, 10 tests pass (migrate idempotente, getLatestMarkets, getCachedBriefing TTL, purgeAndDownsample). Wave 3 (4 agentes, paralelización con baches): T-03a markets (Yahoo v8→v6→stale→empty, ETag, keyless), T-03b gdelt (GDELT 2.0 GeoJSON, contexto NG-2), T-03c news (RSS allowlist SSRF-safe + fast-xml-parser), T-05 core/ai (router cadena íntegra rama claude, briefing cacheado 24h, AGPL-clean). **46 tests conectores + 16 core/ai, typecheck global 5 paquetes verde.** Incidencias resueltas: workflow con `schema` forzado rompía a los especialistas (→ dispatch texto-libre); 2 agentes truncados por límite de turnos (re-dispatch acotado); phantom-dep zod en gdelt (→ guards manuales); empty-RSS news devolvía 2 (→ fix). NOTA proceso: el `schema` forzado en Workflow y prompts largos matan a los subagentes; usar dispatch directo + verify del PfM. Siguiente: Wave 4 = T-04 scheduler.

## [2026-06-13] [PM] [DONE] — **Wave 4 (T-04 scheduler) + fix de integración conectores VERIFICADO**. Scheduler (backend-architect): 4 tiers configurables, deps inyectables, run-immediately+interval, start idempotente, 15 tests. **Gap estructural cazado por el PM**: el bootstrap puso `connectors/tsconfig` con `rootDir:src` pero los conectores viven en `{finance,geo,edu}/` (CLAUDE.md) → el build solo compilaba un skeleton + un duplicado huérfano `src/geo/gdelt.ts` (del 1er workflow fallido), y `connectors/src/index.ts` no re-exportaba nada. FIX PM: borrado `src/` huérfano, creado barrel `connectors/index.ts` (re-exporta fetchMarkets/fetchGdelt/fetchNews + ConnectorResult), `tsconfig` corregido al layout canónico. Verificado: `@www/connectors` exporta el API, 46 tests conectores + 15 scheduler, typecheck global 5 paquetes verde. Siguiente: Wave 5 = T-06 server.ts.

## [2026-06-13] [PM] [DONE] — **Smoke OpenAI en vivo: bug `max_tokens` → `max_completion_tokens`**. El primer boot degradó a cache-fallback; diagnóstico (el catch silencioso tapaba el error real): chain `openai:true` pero la API daba 400 `unsupported_parameter: max_tokens` — gpt-5.4 (modelo nuevo) exige `max_completion_tokens`. FIX en el branch openai del router. Re-test live `complete()` → **"ok"** ✓. El branch claude mantiene `max_tokens` (Anthropic lo usa). 21 tests core/ai + typecheck global verde. LECCIÓN: los tests mockeados no ven errores de la API real; el smoke live es imprescindible.

## [2026-06-13] [PM] [DONE] — **gdelt reescrito (DOC 2.0 + geocode)**. Investigación: GDELT **GEO 2.0 API muerta upstream** (404 en URL exacta de osiris + ejemplo oficial `query=trump`; osiris/worldmonitor tienen issues DESHABILITADOS → sin fix que copiar; osiris usa el mismo endpoint roto). FIX (data-connector-dev): migrado a **DOC 2.0 API** (artlist, funciona) + geocode por país (`country-centroids.ts`, ~60 países → centroides aproximados nivel-país, porque GEO daba puntos reales y murió; país desconocido → lat/lon null). 52 tests connectors + typecheck global verde. Smoke live: el conector maneja HTTP 429 (rate-limit GDELT 1/5s) gracioso → []; en producción (cadencia 15min) sin problema.

## [2026-06-13] [PM] [DONE] — **QA EN VIVO del backend (server arrancado de verdad) — cazó bugs que los tests mockeados NO ven.** OK live: `/api/health` ✓, `/api/markets` ✓ (7 snapshots Yahoo reales: BTC 64023…), news ✓ (88 items persistidos), `/api/briefing` → `null` honesto. BUGS cazados y CORREGIDOS: (1) server default PORT 3001 ≠ proxy vite 8787 → server alineado a 8787; (2) `pnpm dev` (=`tsx server.ts`) **no cargaba `.env`** → `OPENAI_API_KEY` jamás se leía en runtime → OpenAI nunca activaba → FIX `dev = node --env-file-if-exists=.env --import tsx server.ts`; (3) modelo `claude-opus-4-5` **HARDCODEADO** en router.ts:139 + label en briefing.ts → FIX `ANTHROPIC_MODEL` obligatorio sin default (igual que OpenAI). PENDIENTE: conector gdelt HTTP 404 (endpoint GeoJSON incorrecto) → `/api/gdelt` []. Tests verdes tras fixes (21 core/ai + 103 totales). `.env` lo gestiona el usuario (key pegada; el PM no lo toca).

## [2026-06-13] [PM] [DONE] — **Cambio de proveedor IA: Anthropic → OpenAI (ADR-009)**. Router (intel-analyst) gana proveedor `openai` (SDK openai, OPENAI_API_KEY), rama activa del MVP. Cadena ['ollama','openai','groq','claude']. **Modelo OBLIGATORIO sin default** (OPENAI_MODEL; el router falla claro si falta) — corregido tras feedback duro del usuario: NUNCA suponer modelo, familia GPT-4 deprecada (2026). Modelo elegido por usuario = `gpt-5.4`. `.env` creado (OPENAI_API_KEY vacío para que lo pegue el usuario; .env ignorado). 103 tests/0 fail, typecheck global verde. Lección en memoria [never-assume-llm-model]. PENDIENTE: commit del cambio (sin commitear; image.png untracked NO se commitea = captura de facturación).

## [2026-06-13] [PM] [DONE] — **Primer commit (integración con aprobación humana): `269abee` en `main`** — 118 ficheros, 14277 inserciones. Fase 0 (andamiaje + blackboard) + Fase 1 (MVP Finanzas completo y VERIFIED). Árbol limpio. No pusheado (sin remoto configurado / a decisión del usuario).

## [2026-06-13] [VERIFIER + PM] [DONE] — **GATE /verify = VERIFIED. MVP Finanzas (Fase 1) COMPLETO.** El verifier confirmó tests verdes pero truncó por turnos antes del veredicto → el PM completó los chequeos goal-backward por grep. Resultado: typecheck 5 paquetes + server OK; **99 tests / 99 pass / 0 fail**; web vite build OK. Decisiones cumplidas: NO better-sqlite3, NO zod en connectors, NO copia AGPL en core/ai, conectores keyless (sin keys hardcoded), `@libsql/client file://`, capas en config-array (MapView itera LAYERS, addLayer solo dentro del loop), router cadena íntegra ['ollama','groq','claude'] rama claude activa, /api/briefing NO dispara Anthropic, /api/gdelt usa getRecentGdeltEvents, responsive/mobile-first. 1 issue cazado y corregido: briefing no incluía eventos GDELT (D-105) → cableado getRecentGdeltEvents en generateDailyBriefing. Sin stubs/TODO en producto. PENDIENTE usuario: crear `.env` con ANTHROPIC_API_KEY para briefing live (degrada gracioso sin él, R-2). NO commiteado (frontera de integración = aprobación humana).

## [2026-06-13] [PM] [DONE] — **Waves 5+6 COMPLETADAS** (MVP Finanzas implementado al completo). Wave 5 = server.ts (backend-architect): node:http nativo, pipeline origin-check→CORS→rate-limit→SSRF-guard→route, 5 endpoints sólo-lectura del store, /api/briefing NO dispara Anthropic, factory createApp testable, 9 tests. Fix integración: añadido `getRecentGdeltEvents` al store + cableado /api/gdelt (el agente lo flaggeó honestamente en vez de stubear). Wave 6 = web (frontend-dev, truncado por turnos → PM remató 4 fixes de tipos MapLibre v4): Vite+React+MapLibre, config-array central de capas (LAYERS iterado, no imperativo), panel Finanzas con sparkline + loading/empty/error, responsive+mobile-first (ADR-008), lee solo /api/*. **VERIFICACIÓN CONSOLIDADA: typecheck 5 paquetes + server OK, 98 tests/98 pass/0 fail, web vite build OK.** Siguiente: gate `/verify` (verifier, goal-backward) antes de declarar completado.

## [2026-06-13T20:09:18Z] [QA-TESTER] [DONE] — **E2E verificación post-fix contraste cliente↔API: PASS.** Playwright 1.58 headless Chromium. 7/7 instrumentos con precios reales (BTC $64k, SPY $741…), sparkline OK tras click, MapLibre 2 canvas, responsive 375px sin overflow. 0 errores JS / 0 errores red. Warnings WebGL-driver (GPU headless, no app). INFO: `+0.00%` esperado (sin historia previa en DB). INFO: toggle GDELT sin label visible (capa en config-array, esperado NG-2). Reporte: `plans/qa-report-2026-06-13.md`. Screenshots: `plans/screenshots/`.

## [2026-06-13] [BACKEND-ARCHITECT + PM] [DONE] — **Wave 1 / T-01 (Bootstrap) COMPLETADO y VERIFICADO**. Scaffolding por `backend-architect` (2 dispatches murieron por límite de turnos ~30 creando ficheros uno a uno → el PM remató install/aprobación-de-builds/verify directamente, infra no código de producto). Estado: pnpm workspace (6 proyectos), 5 paquetes `@www/*` con skeletons+deps, `.venv` Python en `tools/py/`, `data/`, `.gitignore` (+`.venv/`). Verify VERDE: `pnpm install` exit 0; **R-1 SUPERADO** (libsql nativo ejecuta `select 1`→`[{"ok":1}]` en Windows); @anthropic-ai/sdk carga; `tsc --noEmit` 5 paquetes exit 0; test-runner `node --import tsx` OK. Fix PM: `allowBuilds: esbuild: true` en pnpm-workspace.yaml (hook supply-chain) + `declarationMap:false` en web/tsconfig (TS5069). Pendiente del usuario: crear `.env` desde `.env.example` con `ANTHROPIC_API_KEY` (hooks bloquean que lo haga un agente). Siguiente: Wave 2 = T-02 (store).

## [2026-06-13] [PM] [DONE] — **Fix cold-start del briefing + FASE 1 CERRADA.** Race confirmado en vivo: el job `daily` leía el store vacío al arrancar (corría en paralelo con los conectores) → 1er briefing sin datos. FIX (backend-architect): `createScheduler` espera fast/medium/slow antes de daily en el boot; intervalos posteriores sin cambios. 16 tests scheduler + typecheck global verde. Commit `4e46e9d`. **Fase 1 (MVP Finanzas) 100% cerrada**: 10 commits (269abee…4e46e9d), QA E2E PASS, OpenAI gpt-5.4 live, briefing real con datos. Follow-ups abiertos (no bloqueantes): gdelt se puebla con la cadencia 15min (mi probing agotó el rate-limit); puntos gdelt = centroides nivel-país (GEO API real murió). Siguiente: **Fase 2** (RPI Research+Design).

## [2026-06-13] [SYSTEM-ARCHITECT] [DONE] — **Fase 2 / slice CII: design-doc creado** `docs/design/2026-06-13-cii-scoring.md` (registrado por el PM). Metodología CII de worldmonitor RE-IMPLEMENTADA clean-room (ADR-002; pesos propios, `methodology_version='cii-core-1'`, NO copió fuente AGPL — ni leyó el .mdx completo para evitar contaminación). **HALLAZGO CRÍTICO**: el GDELT actual NO es señal de conflicto (query financiera, severity null, país=del medio no del suceso) → modelado honesto como "presión informativa financiera por país-fuente", NO Unrest/Conflict. **PHASE-SPLIT obligatorio** (realidad de datos): solo el componente **Information** está activo; Conflict/Security/Unrest = registrados-pero-degradados (signal-presence model), se desbloquean aterrizando sus conectores keyed (Conflict→ACLED/UCDP, Security→militar/GPS, boosts→USGS/OFAC/FIRMS/AIS). Histórico = tabla `cii_snapshots` + migración 002 + dynamicScore (delta vs 24h). 18 D-NNN, 7 GAP, 8 OQ. DONE_WITH_CONCERNS: el PM debe RATIFICAR el phase-split + OQs (esp. OQ-6: derivar los ~65 coeficientes editoriales con intel-analyst) antes del plan. Siguiente RPI: Plan → /check-plan.

## [2026-06-13] [PM] [IN_PROGRESS] — **Fase 2 REORDENADA (ADR-010): capa de EVENTOS GLOBALES multi-fuente = 1ª rebanada (CII después).** El usuario aclaró que el núcleo es info de eventos de TODO el mundo que afecten economía+seguridad (disturbios/revueltas/guerras/manifestaciones + terremotos/inundaciones/incendios…), estilo osiris/Palantir/worldmonitor. Lanzado workflow `wf_aeb994ab-a59`: **verificación EN VIVO en paralelo** de 5 fuentes candidatas (USGS terremotos, NASA EONET naturales, GDELT raw Events CSV, ReliefWeb desastres, ACLED/UCDP conflicto) → system-architect sintetiza `docs/design/2026-06-13-global-events.md` (modelo de evento unificado, conectores keyless-first, schema time-series, capas de mapa por tipo, alimenta briefing + CII). Lección aplicada: verificar fuentes antes de diseñar (GDELT GEO murió por asumir).

## [2026-06-14] [SYSTEM-ARCHITECT + PM] [DONE] — **Design-doc capa de eventos globales creado** `docs/design/2026-06-13-global-events.md` (57KB, 10 secciones, 17 D-NNN, PLANNING COMPLETE; registrado por el PM). Diseñado sobre 5 fuentes **verificadas EN VIVO** (workflow `wf_aeb994ab-a59`). **MVP de la capa = 3 conectores keyless+geo-real+ligeros**: USGS earthquakes (coords epicentro, mag/alert/sig/tsunami, Public Domain), NASA EONET (13 categorías naturales: incendios/volcanes/tormentas/inundaciones…, coords reales), GDELT 2.0 **raw Events CSV** (conflicto/político con coords del SUCESO + CAMEO/QuadClass/Goldstein — reemplaza la GEO API muerta y supera al DOC actual). **DIFERIDAS con razón**: ReliefWeb (appname pre-aprobado OCHA, sin lat/lon NG-3), UCDP (token email NG-4), ACLED (DNS no resuelve + ToS no verificable → guardrail feedback_data_tos NG-4), GDELT GKG (pesado 1.86MB NG-5). Diseño: **tabla `events` unificada** (reemplaza `gdelt_events` vía migración) + normalización de severity 0..100 entre fuentes heterogéneas + tiers scheduler + capas MapLibre por tipo (D-008) + `/api/events` + alimenta briefing y los componentes Conflict/Security/Unrest del CII. Siguiente RPI: Plan → /check-plan.

## [2026-06-14T00:30:00Z] [DATA-CONNECTOR-DEV] [DONE] — **Verificacion en vivo GDELT 2.0 raw Events CSV (fact-check, sin ficheros de produccion)**. lastupdate.txt HTTP 200 keyless. ZIP export 15min = 26 KB comprimido / 166 KB descomprimido / 415 registros. Campos confirmados en vivo: ActionGeo_Lat (col 56) + ActionGeo_Long (col 57) = coords REALES del suceso (97.6% cobertura geo en batch verificado). EventCode CAMEO, QuadClass (1-4), GoldsteinScale [-10..+10], AvgTone, Actor1/Actor2 (nombre+pais), ActionGeo_FullName+CountryCode, SOURCEURL. Sin muertes/magnitud fisica (eso es USGS/ACLED). ToS: https://www.gdeltproject.org/about.html — "unlimited and unrestricted use"; atribucion requerida (citar GDELT + link). Keyless. ETag en cabecera ZIP (soporta If-None-Match). Cadencia 15min alineada con scheduler. VEREDICTO: RECOMENDADA — geo REAL por evento (supera al DOC 2.0 actual que solo da centroide-pais), keyless, 26 KB/batch.

## [2026-06-14T00:00:00Z] [DATA-CONNECTOR-DEV] [DONE] — **Verificación en vivo ACLED + UCDP (conflicto armado). Ambas requieren key/registro — NINGUNA es keyless.** UCDP: HTTP 401 confirmado en vivo (`API token required. Add header: x-ucdp-access-token`). Registro GRATUITO vía email a mertcan.yilmaz@pcr.uu.se (nombre+afiliación+uso); 3-5 días hábiles; 5000 req/día; datos CC BY 4.0 (atribución requerida). Trae lat/lon por evento, type_of_violence, deaths_a/b/civilians, date_start, country, side_a/side_b. JSON paginado. Versión actual: 26.1 (no 24.1 del prompt). ACLED: DNS no resuelve desde esta máquina (`Could not resolve host: api.acleddata.com`); el sitio confirma registro gratuito con cuenta myACLED (formulario: nombre+email+org+categoría); categorías incluyen "individual/personal" (no solo académico); ToS no verificado completamente (página principal 404). Veredicto: UCDP = DIFERIDA (requiere key gratuita, registro manual 3-5 días); ACLED = DIFERIDA + BLOQUEADA (DNS falla en este entorno, ToS no plenamente verificable). Recomendación al PM/architect: si el usuario puede registrar token UCDP, es la mejor opción de conflicto (geo real, CC BY 4.0, 5k req/día).

## [2026-06-14] [PM] [DONE] — **Plan de la capa de eventos globales escrito + `/check-plan` = PASS.** Plan `plans/2026-06-14-global-events.md` (8 tareas T-08..T-14, 5 waves) derivado del design-doc `2026-06-13-global-events.md`. Decisiones internas ratificadas OQ-1..8; **OQ-2 = DROP sin migrar** (decisión del usuario: el histórico financiero de `gdelt_events` no se copia, dato trivial severity-null/centroide-medio; elimina R-6). 5 **correcciones de realidad** verificadas en disco: C-1 migración = `002_events.sql` (solo existe 001; el 002 del CII nunca se construyó), C-2 `purgeAndDownsample` borra `gdelt_events` → repuntar a `events`, C-3 `getRecentGdeltEvents` re-cableado a `events WHERE source='gdelt'` (retro-compat mantiene server.ts/briefing.ts verdes), C-4 `export.CSV.zip` = ZIP → extracción zero-dep `zlib.inflateRawSync` + parse manual local-header, C-5 barrel `connectors/index.ts` = fichero compartido → lo actualiza el PM post-wave B.

## [2026-06-14] [PLAN-CHECKER] [DONE] — Plan `plan-global-events`: **PASS** (0 issues bloqueantes, 4 warnings no-bloqueantes). 5 dimensiones limpias (cobertura G-1..9, completitud verify<60s, grafo acíclico A→B→C→D→E con disjunción real de ficheros, scope sin frases de erosión, riesgos R-1..8+ZIP+retrocompat con dueño) + fidelidad de decisiones bloqueadas (no-AGPL D-006, @libsql/client ADR-006, openai sin proveedor nuevo ADR-009, config-array iterado D-005/008, persistir-antes-de-servir ADR-004) + correcciones de realidad C-1..C-5 verificadas fichero a fichero. Warnings: (1) T-10c ZIP zero-dep = pieza más frágil, revisar Self-Report; (2) scope multi-área esperado (flujo punta a punta, particionado en 5 waves); (3) responsive de T-13 se delega al qa-tester; (4) vigilar que T-09 no importe @www/store. Informe en `plans/check-plan-global-events-2026-06-14.md`. Registrado por el PM (plan-checker es read-only).

## [2026-06-14] [PM] [DONE] — **Wave A COMPLETA y VERIFICADA (verify propio del PM, no relay).** Usuario aprobó arrancar. T-08 (store, backend-architect) + T-09 (severity, intel-analyst) en paralelo, ficheros disjuntos. **T-08**: `migrations/002_events.sql` (crea `events` + 4 índices, DROP `gdelt_events` sin migrar OQ-2), `EventRow`/`EventFilter`, `upsertEvents`/`getEvents`/`getEvent`/`getEventsByCountry`, C-2 `purgeAndDownsample` repuntado a `events` (COALESCE(occurred_at,captured_at)), C-3 `getRecentGdeltEvents` lee `events WHERE source='gdelt'` mapeado a forma legacy → server.ts/briefing.ts intactos. **32/32 tests verde** (UPSERT actualiza-no-duplica, filtros type/category/minSeverity/since/bbox/limit, migración idempotente 3×, gdelt_events dropada). El backend-architect truncó por límite de turnos mientras diagnosticaba 2 fallos en un helper de test (`country: ?? 'ES'` convertía null→'ES'); había aplicado el fix (`'country' in overrides`) antes de truncar → el PM corrió el verify final. **T-09**: `geo/severity.ts` severityUsgs/Eonet/Gdelt 0..100 clamp duro, valores editoriales propios re-derivados (no-AGPL), sin import @www/store, sin dep nueva. **36/36 tests verde**. **Global `pnpm -w exec tsc --noEmit` EXIT 0** (cross-package OK: insertGdeltEvents legacy aún compila — lo reemplaza T-11). Siguiente: checkpoint usuario → Wave B (T-10a usgs ‖ T-10b eonet ‖ T-10c gdelt-refactor).

## [2026-06-14] [PM] [DONE] — **Wave B COMPLETA y VERIFICADA.** 3 conectores keyless geo-real en paralelo (data-connector-dev ×3, ficheros disjuntos, write-only + test propio). **T-10a usgs** (significant_week+all_day GeoJSON, dedup por feature.id, severityUsgs, If-Modified-Since, nearest-centroid) 15/15. **T-10b eonet** (v3 geojson limit=20 R-4, 7 categorías→event_type, descarta earthquakes EONET, severityEonet, closed en raw_json) 26/26. **T-10c gdelt REFACTOR** (DOC artlist→raw Events CSV: lastupdate.txt→export.CSV.zip→**ZIP zero-dep `zlib.inflateRawSync`+local-file-header parse**, soporta deflate+stored, CSV 61-col TAB por índice fijo, R-3 valida ==61 cols, coords reales ActionGeo del suceso, EventCode 14x→protest resto→conflict, severityGdelt) 35/35. La escotilla fflate-ADR quedó codeada pero NO necesaria. **Consolidado: 148/148 tests conectores, connectors tsc limpio, global tsc EXIT 0.** Barrel `connectors/index.ts` cableado por el PM post-wave (fetchUsgs/fetchEonet + fetchGdelt ahora ConnectorResult<EventRow>). **FIX INTEGRACIÓN PM (gotcha cross-package):** el tsc consolidado cazó `@www/store has no exported member EventRow` — los `@www/*` resuelven tipos cross-package vía **`dist` construido** (package.json `types: ./dist/index.d.ts`), NO `src`; tras cambiar el API público de un paquete hay que **rebuild** (`pnpm --filter @www/store build`) o el downstream ve tipos stale. + `exactOptionalPropertyTypes`: UsgsParams/GdeltParams necesitaban `| undefined` explícito (los conectores pasan `properties.<campo>` que es `number|undefined`). Tests tsx per-file NO ven esto (transpilan, no typecheck) → imprescindible el tsc consolidado del PM. Siguiente: checkpoint usuario → Wave C (T-11 scheduler jobs usgs/eonet/gdelt→upsertEvents).

## [2026-06-14] [PM] [DONE] — **Wave C COMPLETA y VERIFICADA.** T-11 scheduler (backend-architect): SchedulerDeps gana fetchUsgs/fetchEonet + upsertEvents; fetchGdelt→ConnectorResult<EventRow>; **insertGdeltEvents ELIMINADO** de SchedulerDeps/REAL_STORE_AI_DEPS/jobs (gdelt_events dropada; gdelt ahora→upsertEvents). defaultJobs devuelve [markets(fast), usgs(fast), eonet(medium), gdelt(medium), news(slow), daily] — el job gdelt financiero reemplazado por el de eventos; firma `defaultJobs(cfg?,deps?):Job[]` intacta (server.ts sin cambios). Boot-sequencing (no-daily→await→daily) + daily(generateDailyBriefing+purgeAndDownsample→events) conservados. **24/24 tests, scheduler tsc limpio, scheduler dist rebuilt, global tsc EXIT 0.** El agente creó memoria [[project_scheduler_t11]]. Siguiente: checkpoint usuario → Wave D (T-12 server.ts /api/events + /api/events/:source/:id, retro-compat /api/gdelt).

## [2026-06-14] [PM] [DONE] — **Wave D COMPLETA y VERIFICADA.** T-12 server.ts (backend-architect): `GET /api/events` (parsea EventFilter del querystring: type/category(validado natural|conflict)/since→sinceMs/minSeverity/limit/bbox(4 floats, ignora si inválido), solo params presentes) + `GET /api/events/:source/:id` (regex, raw_json parseado a objeto con fallback al string, 404 si null) — colocada ANTES de /api/events (más específica primero, patrón /api/markets/:symbol). Pipeline middleware (origin-check→CORS→rate-limit→SSRF-guard→route) INTACTO. SOLO-LECTURA, ningún conector on-request (ADR-004/D-107). /api/gdelt sin cambios (retro-compat vía getRecentGdeltEvents→events source='gdelt'). **26/26 tests (17 nuevos + 3 regresiones Fase 1 health/markets/briefing), global `pnpm -w exec tsc --noEmit` limpio.** Siguiente: checkpoint usuario → Wave E (T-13 web capas+panel ‖ T-14 briefing riesgo global).

## [2026-06-14T00:00:00Z] [DATA-CONNECTOR-DEV] [DONE] — **Verificacion en vivo USGS Earthquakes (fact-check, sin ficheros de produccion)**. Endpoints: all_day.geojson HTTP 200 (150 KB, 210 eventos, 470ms); significant_week.geojson HTTP 200 (3.4 KB, 4 eventos, 170ms). Keyless — zero-key confirmado. ToS verificado: datos USGS = U.S. Public Domain (https://www.usgs.gov/information-policies-and-instructions/copyrights-and-credits); atribucion solicitada con credito "U.S. Geological Survey". Sin API key ni registro. Cache-Control: public, max-age=60 (sin ETag, con Last-Modified — soporta If-Modified-Since). GeoJSON estandar con lat/lon/profundidad reales por evento. Veredicto: RECOMENDADA para MVP global-events (keyless + geo real + ligera). Detalle completo en respuesta al PM.

## [2026-06-14T13:42:00Z] [DATA-CONNECTOR-DEV] [DONE] — **T-10b eonet.ts COMPLETO. 26/26 tests verdes.** Ficheros: `packages/connectors/geo/eonet.ts` (352 lineas) + `packages/connectors/geo/eonet.test.ts` (502 lineas). Fuente: NASA EONET v3 GeoJSON keyless — dominio publico 17 U.S.C. §105. ToS verificado: https://eonet.gsfc.nasa.gov/docs/v3 — uso programatico libre sin clave; atribucion "Data: NASA EONET"; disclaimer "visualization only". Patron osiris completo: fetch + User-Agent custom + AbortSignal.timeout(8000) + fallback multinivel (HTTP no-OK, timeout/red, shape invalido) + serve-stale (single-flight + lastGood TTL 1h) + retorno vacio gracioso NUNCA lanza. Mapeo D-102: wildfires→wildfire, volcanoes→volcano, severeStorms→storm, floods→flood, landslides→landslide, drought→drought, tempExtremes→tempExtreme. Earthquakes EONET DESCARTADOS (USGS es la fuente sismica). severity=severityEonet(eventType, magnitudeValue, magnitudeUnit) — clamp 0..100 verificado. lat/lon de geometry[].coordinates [lon,lat]. country=nearest-centroid o null (R-8). raw_json incluye closed (eventos cerrados incluidos). Import EventRow desde @www/store. NO toque barrel index.ts (PM post-wave B). Self-Report: real.

## [2026-06-14] [PM] [DONE] — **Wave E (FINAL) COMPLETA y VERIFICADA. Las 8 tareas de la rebanada implementadas.** T-13 web (frontend-dev): capas por tipo en `layers.config.ts` (evt-earthquake/wildfire/volcano/storm/flood/conflict/protest, source 'events', circle+heatmap por severity, sustituyen las legacy gdelt-events), `EventsPanel.tsx` nuevo (lista top por severidad + toggles por category + estados loading/empty/error + atribución USGS/NASA/GDELT), `client.getEvents`, MapView registra source 'events', App responsive mobile-first. **web tsc + vite build EXIT 0** (el agente truncó en el reporte final como en Fase 1 → el PM corrió el verify: limpio). T-14 briefing (intel-analyst): `buildGlobalRiskContext(EventRow[])` (top-10 por severity, '' si vacío), `serializeContext(latest, events:EventRow[])` (reemplaza bloque GDELT-financiero legacy), `generateDailyBriefing` alimenta vía `getEvents({sinceMs:now-48h, minSeverity:20, limit:50})`, contrato caché D-106 intacto, sin proveedor LLM nuevo (ADR-009). **27/27 tests.** **CONSOLIDADO PM: web build OK, core-ai dist rebuilt, global `pnpm -w exec tsc --noEmit` EXIT 0, SUITE COMPLETA 257/257 tests / 0 fail** (store 32 + connectors 148 + scheduler 24 + core-ai 27 + server 26). Siguiente: gate `/verify` (verifier goal-backward) + smoke EN VIVO (curl /api/events + qa-tester 375/1200px) ANTES de declarar la rebanada VERIFIED (lección: verde ≠ funciona).

## [2026-06-14T10:00:00Z] [QA-TESTER] [BLOCKED] — **E2E capa eventos globales: BLOQUEADO (Bash denegado) + BUG CRITICO detectado por analisis estatico.** Bash permission denied en este contexto → no puedo arrancar backend/frontend ni ejecutar Playwright en vivo. DETECTADO sin ejecutar: **BUG CRITICO — contrato camelCase/snake_case roto entre /api/events y client.ts**. El store serializa `EventRow` camelCase (`sourceEventId`, `eventType`, `capturedAt`, `occurredAt`, `rawJson`) pero `RawEventRow` en `packages/web/src/api/client.ts:56-71` lee snake_case (`source_event_id`, `event_type`, `captured_at`, `occurred_at`, `raw_json`). Consecuencias en runtime: (1) `key: usgs:undefined` para todos los eventos → React key collision; (2) `event_type` = undefined → TODOS los filtros de capa MapLibre `['==', ['get', 'event_type'], X]` NUNCA matchean → NINGÚN punto aparece en el mapa aunque la data llegue; (3) `capturedAt = "Invalid Date"` → sort del panel roto. lat/lon sí coinciden (mismo nombre) → eventos SÍ pasan el guard de `adaptEventRow` pero están malformados. FIX requerido (Auto-fix Regla 1): actualizar `RawEventRow` en client.ts a camelCase (sourceEventId, eventType, capturedAt, occurredAt, rawJson) y los accesos en `adaptEventRow`. Espero aprobación del usuario para ejecutar Bash (arrancar entorno + Playwright). Ver qa-report-2026-06-14.md para evidencia completa.

## [2026-06-14] [VERIFIER+PM] [DONE] — **GATE /verify = VERIFIED (código) + SMOKE EN VIVO = PASS (backend/API). qa-tester (UI) en curso.** El verifier truncó por turnos antes del veredicto (igual que Fase 1) → el PM completó el goal-backward. **Build/tests**: global `pnpm -w exec tsc --noEmit` EXIT 0; **257/257 tests** (store 32 + connectors 148 + scheduler 24 + core-ai 27 + server 26); web vite build OK. **Goal-backward G-1..G-9 cumplidos en código real**: G-1 migración 002 dropa gdelt_events + getRecentGdeltEvents retro-compat; G-2 3 conectores keyless→upsertEvents; G-3 severity 0..100 re-derivado; G-4 UPSERT+índices+purge; G-5 tiers usgs-fast/eonet-gdelt-medium; G-6 capas por tipo en config-array (MapView itera LAYERS, addLayer SOLO en el loop líneas 116-138, sin imperativo); G-7 /api/events solo-lectura+atribución; G-8 briefing buildGlobalRiskContext sin proveedor nuevo; G-9 getEventsByCountry (cii.bridge). Sin TODO/FIXME/catch-vacío/placeholder en ficheros nuevos. Severity no-AGPL (valores propios comentados). **SMOKE EN VIVO (server real :8787, conectores live)**: boot migró 002, **usgs persistió 245 eventos** (sev 90, coords reales Philippines/etc), **gdelt 650** (coords ActionGeo del suceso, ej. India 28.6/75.0), news 89, markets 7, **briefing live model=openai/gpt-5.4** leyendo eventos. `/api/events` ✓ (filtros type/minSeverity: minSeverity≥50 → 174 eventos usgs+gdelt), `/api/events/usgs/us7000srb1` ✓ (raw_json parseado a objeto), `/api/gdelt` retro-compat ✓ (forma legacy GdeltEvent), 404 ✓. **FOLLOW-UPS HONESTOS (no bloquean)**: (1) **EONET devolvió 0 en vivo** — upstream transitorio (R-4 503), el conector degradó gracioso (vacío); 26 tests con fixtures verdes; re-check próximo ciclo scheduler. (2) **GDELT QuadClass 1/2 (cooperación) se etiqueta category='conflict' con severity baja** (ruido CAMEO): mitigado por minSeverity (briefing usa minSeverity:20; mapa/UI pueden filtrar) — calibración OQ-3/OQ-4 pendiente. Siguiente: qa-tester UI 375/1200px → si PASS, declarar rebanada VERIFIED + marcar ROADMAP.

## [2026-06-14] [QA-TESTER] [BLOCKED→FIXED] — **Bash denegado en el contexto del qa-tester → no pudo correr Playwright, PERO por análisis estático cazó BUG CRÍTICO de contrato** (la clase de bug de [[feedback-live-qa-vs-mocks]]). BUG-1: `packages/web/src/api/client.ts` `RawEventRow` usaba snake_case (`event_type`/`source_event_id`/`captured_at`/`occurred_at`/`raw_json`) pero el wire de `@www/store` es **camelCase** (EventRow serializado directo, sin transform). `adaptEventRow` leía `undefined` en todo → `event_type=undefined` → filtros MapLibre `['get','event_type']` NUNCA matchean → **0 puntos en el mapa** + key colisión + Invalid Date. Los 257 tests no lo vieron (no cubren store→server→client-js HTTP). Memoria [[feedback_api_contract_camelcase]].

## [2026-06-14T20:35:00Z] [DATA-CONNECTOR-DEV] [DONE] — **Verificacion en vivo GDELT GKG v2 (fact-check, sin ficheros de produccion).** Batch 20260614204500: HTTP 200 keyless, ZIP 2.75 MB / CSV 8.46 MB / 670 registros / cadencia 15min. Cache-Control: public max-age=3600; ETag soportado (If-None-Match). Layout 27 columnas TAB: col1=GKGRECORDID, col2=DATE(YYYYMMDDHHMMSS), col3=SourceCollectionIdentifier, col4=SourceCommonName, col5=DocumentIdentifier(SOURCEURL), col6=V1Counts, col7=V2Counts, col8=V1Themes(;sep), col9=V2EnhancedThemes(tema,charoffset), col10=V2Locations(tipo#nombre#cc#adm1#lat#lon#featureid), col11=V2EnhancedLocations, col12=V2Persons, col13=V2Organizations, col14=V2EnhancedOrganizations, col16=V2Tone(tone,pos,neg,polarity,ard,selfDens,wordCount), col18=GCAM(clave:valor;…). Geo cobertura col10: 497/670 (74.2%) tienen lat/lon. V2Tone = 7 valores float: AvgTone, PositiveTone, NegativeTone, Polarity, ActivityDensity, SelfDensification, WordCount. ToS: "unlimited and unrestricted use" + citacion requerida (https://www.gdeltproject.org/). Keyless. V2Themes reales observados: WB_2433_CONFLICT_AND_VIOLENCE, WB_2462_POLITICAL_VIOLENCE_AND_WAR, ECON_STOCKMARKET, EPU_CATS_TAXES, EPU_CATS_MIGRATION_FEAR_FEAR, WB_507_ENERGY_AND_EXTRACTIVES, ENV_OIL, ENV_CLIMATECHANGE, MANMADE_DISASTER_IMPLIED, NATURAL_DISASTER_FLOODED, SOC_GENERALCRIME, CRISISLEX_C03_WELLBEING_HEALTH, CRISISLEX_C07_SAFETY. DIFERENCIA vs raw Events CSV: GKG es nivel-ARTICULO (1 fila = 1 URL), no nivel-evento-diádico; tiene geo 74% frente a 97.6% del Events CSV; la riqueza es tematica+tonal+entidades, NO coordenadas de evento. VEREDICTO: GKG es el mejor radar tematico keyless (geo+tono+temas+entidades+GCAM), pero NO reemplaza al Events CSV para coordenadas de suceso — son complementarios. Para un "radar tematico" (que es la pregunta), GKG v2 es VIABLE y RECOMENDADO con: col5=URL, col8+col9=themes, col10=lat/lon (74%), col12=persons, col13+14=orgs, col16=tone, col18=GCAM. El conector requiere zlib.inflateRawSync (igual que el Events ZIP) + parse TAB + parser de subdelimitadores (#, ;, ,).

## [2026-06-14] [PM] [DONE] — **BUG-1 FIXEADO + REBANADA VERIFIED en todos los niveles.** FIX (PM, verificado contra el wire real por curl=camelCase): `RawEventRow` + `adaptEventRow` en `client.ts` a camelCase (`sourceEventId`/`eventType`/`occurredAt`/`capturedAt`/`rawJson`). Cadena coherente: `GlobalEvent.eventType` → `eventsToGeoJSON` `properties.event_type` → `filterExpr ['get','event_type']`. web tsc+build OK. **Verificación en vivo del contrato** (server real, simulando adaptador): event_type undefined=0, key :undefined=0, Invalid Date=0. **BROWSER E2E (Playwright, lo corrió el PM porque el qa-tester tenía Bash bloqueado — `events-e2e.mjs`)**: events_render_desktop PASS (canvas + **30 filas** render, types [conflict,protest], badTypes=0, atribución ✓), toggle PASS (30→0 al apagar), responsive_mobile PASS (375px sin overflow, drawer abre), **0 errores consola, 0 errores red, VERDICT PASS**. Screenshots en `plans/screenshots/events-*.png`. **REBANADA 1 DE FASE 2 (CAPA DE EVENTOS GLOBALES) CERRADA Y VERIFIED**: /verify goal-backward + 257/257 tests + global tsc + web build + smoke API en vivo (usgs 245/gdelt 650/briefing gpt-5.4) + browser E2E. ROADMAP marcado (11/16, 69%). LECCIÓN reforzada: el qa-tester E2E NO es sustituible por tests verdes ni por /verify estático — cazó un bug de contrato que 257 tests + tsc + verifier + smoke-API-curl NO vieron (el curl ve el wire correcto; el bug estaba en el PARSER del cliente). PENDIENTE: commit (aprobación humana); borrar image.png (sin trackear). FOLLOW-UPS no-bloqueantes: EONET re-check próximo ciclo; ruido GDELT QuadClass1/2 (calibración OQ-3/OQ-4).

## [2026-06-14] [PM] [IN_PROGRESS] — **Fase 2 rebanada 2 = RADAR GEOECONÓMICO TEMÁTICO (ADR-011). Brainstorming + verificación en vivo COMPLETOS; dirección aprobada por el usuario.** El usuario quiere ver TODO lo que afecte a la economía mundial más allá de finanzas (revueltas/cambios de gobierno, materias primas, tierras raras, IA/tech, semis, ataques a data centers, comercio/sanciones). Brainstorming fijó: **radar dedicado por tema** · fuente **GKG backbone + news curada** · **6 secciones** (inestabilidad política · materias primas&energía · tierras raras&minerales críticos · semis/IA/tech · infra digital&ciber · comercio&sanciones) · **atado al mapa** (tema filtra eventos geo + abre sección titulares+tendencia). **Verificación en vivo `wf_e68c43c8-11c`** (4 agentes): GKG v2 keyless viable (670 art/15min, 2.75MB, ETag, reusa extractZipFirstEntry; temas WB_*/ENV_*/ECON_* + tono V2Tone + entidades + geo 74%). 2 hallazgos de diseño: (1) cobertura desigual — tierras-raras/semis/data-centers/ciber sin código → derivar por keyword/entidad; (2) geo del GKG = del artículo no del suceso → "inestabilidad política" reusa events geo-reales, el resto = señales temáticas. Arquitectura recomendada y aprobada: conector NUEVO `gkg.ts` (27 cols), tabla NUEVA `signals` (article-level, separada de `events`), clasificación theme-codes+keyword (editorial no-AGPL), tendencia=volumen+AvgTone por ventana. Siguiente RPI: **dispatch system-architect → design-doc `docs/design/2026-06-14-geoeconomic-radar.md`** → plan → /check-plan → implementar. La síntesis cross-tema (convergencia) sigue Non-Goal.

## [2026-06-14T21:30:00Z] [SYSTEM-ARCHITECT] [DONE] — design-doc: docs/design/2026-06-14-geoeconomic-radar.md (## PLANNING COMPLETE). **Radar geoeconómico temático (ADR-011)** diseñado sobre los hechos GKG verificados EN VIVO (`wf_e68c43c8-11c`): conector NUEVO `gkg.ts` (27 cols TAB + subdelimitadores `;`/`#`/`,`, **REUSA `extractZipFirstEntry`** de gdelt.ts, título de V2ExtrasXML PAGE_TITLE, geo best-effort V2Locations tipo 3/4) + tabla NUEVA `signals` + tabla puente `signal_sections` (migración `003_signals.sql`, **SEPARADA de events**, dedup por GKGRECORDID) + clasificador editorial `sections.config.ts` (el corazón: mapa sección→{themeCodes,keywords,entityHints} re-derivado no-AGPL, classify→0..N secciones con matchedBy para auditoría) + API store (upsertSignals/getSignals/getSignalTrend, tendencia=volumen+AvgTone/ventana) + job gkg→medium (NO toca el job gdelt) + endpoints /api/signals + /api/signals/trend + panel Radar atado al mapa (6 secciones; political_instability REUSA events geo-reales, las otras 5 = capas signals por sección en el config-array) + ampliación allowlist SSRF feeds RSS temáticos. **8 D bloqueadas (D-001..008) + 8 internas (D-200..207, sin colisión con D-100..108 de la rebanada 1), 8 Non-Goals (convergencia cross-tema NG-1, CII NG-2, ML cliente NG-3, GCAM NG-5), 7 GAP + 8 OQ.** Non-AGPL respetado (D-008, como severity.ts). spec-validator: sin rechazo al guardar. **OQ que el PM debe RATIFICAR antes del plan**: OQ-1 (tabla puente signal_sections vs columna JSON), OQ-4 (calibración del clasificador con intel-analyst — área de mayor riesgo, R-1/GAP-4), **OQ-8 (feeds RSS temáticos concretos NO verificados en esta sesión — recomiendo entregar radar con GKG primero y diferir feeds RSS a iteración incremental con ToS verificado)**. Siguiente RPI: Plan → /check-plan (gate PREVIO, NUNCA implementación directa).

## [2026-06-14] [PM] [DONE] — **Plan del radar geoeconómico escrito + `/check-plan` = PASS.** Plan `plans/2026-06-14-geoeconomic-radar.md` (6 tareas T-15..T-20, 5 waves) derivado del design-doc. OQs ratificadas 8/8; **OQ-8 = GKG primero, RSS después (decisión del usuario — feeds sin ToS verificado, guardrail)**. Lecciones de la rebanada 1 horneadas: L-1 contrato web camelCase (anti-BUG-1), L-2 rebuild dist cross-package, L-3 tsc consolidado del PM, L-4 barrel=PM, L-5 smoke+browser-E2E, L-6 dispatch directo. Waves: A=store+migración 003 ‖ clasificador sections.config.ts · B=conector gkg.ts+util zip.ts (refactor gdelt sin regresión) · C=job gkg→medium · D=server /api/signals+trend · E=RadarPanel+capas+map-tie.

## [2026-06-14] [PLAN-CHECKER] [DONE] — Plan `geoeconomic-radar`: **PASS** (0 issues bloqueantes, 3 warnings no-bloqueantes). 5 dimensiones limpias (cobertura G-1..7 + G-8 diferido coherente por OQ-8; completitud verify<60s; grafo acíclico A→E con disjunción real, T-16 sin import @www/store preserva paralelismo Wave A; scope 5-áreas = rebanada vertical aceptada; riesgos con dueño) + fidelidad bloqueadas (no-AGPL D-008, @libsql/client, ADR-004, signals SEPARADA de events, job gkg sin tocar gdelt) + L-1 camelCase verificado contra client.ts/server.ts reales. **3 warnings (el PM aplicó los 2 técnicos al plan):** W-1 scope 5-áreas (aceptado); **W-2 trampa migrate.ts** (`sql.split(';')` descarta chunks que empiezan por `--` → comentarios de 003_signals.sql NO deben preceder un statement en el mismo chunk + test assert sqlite_master) → añadido a T-15; **W-3 contrato mapa** (`SignalRow.sections` es array, MapLibre `['get','section']` no indexa arrays → emitir 1 feature por señal×sección con `section` escalar, evita un 2º BUG-1) → añadido a T-20. Registrado por el PM (plan-checker es read-only). Siguiente: aprobación del usuario → implementar wave a wave.

## [2026-06-14] [PM] [DONE] — **Wave A (radar) COMPLETA y VERIFICADA.** Usuario aprobó arrancar. T-15 store (backend-architect) + T-16 clasificador (intel-analyst) en paralelo, disjuntos. **T-15**: `migrations/003_signals.sql` (signals + signal_sections puente + 4 índices, NO toca events), `Section`/`SignalRow`(camelCase)/`SignalTrendPoint`, `upsertSignals`/`getSignals`/`getSignalTrend` (tendencia volumen+AvgTone/bucket), `purgeAndDownsample` extendido a signals. **W-2 evitado**: el test assertea `sqlite_master` (los CREATE no se descartan por el split(';') del runner). **49/49 tests**. **T-16**: `geo/sections.config.ts` — SECTIONS (6 secciones, theme-codes+keywords+entityHints re-derivados no-AGPL) + `classify()` puro (0..N secciones, matchedBy theme>keyword>entity), **sin import @www/store** (Section union local, preserva paralelismo Wave A). **37/37 tests**. **Consolidado PM: store dist rebuilt (L-2), connectors tsc limpio (T-16), global `pnpm -w exec tsc --noEmit` EXIT 0.** Siguiente: checkpoint usuario → Wave B (T-17 conector gkg.ts + util zip.ts compartida + refactor gdelt sin regresión).

## [2026-06-14T23:10:00Z] [INTEL-ANALYST] [DONE] — **T-16 sections.config.ts COMPLETADO. 37/37 tests verdes. tsc limpio.** Ficheros: `packages/connectors/geo/sections.config.ts` (394 líneas) + `packages/connectors/geo/sections.config.test.ts` (478 líneas). Función PURA: sin import @www/store, sin deps nuevas, Section local como union de 6 literales (espejo estructural de @www/store T-15). SECTIONS: 6 secciones con themeCodes+keywords+entityHints documentados, re-derivados del codebook GKG público (LOOKUP-GKGTHEMES.TXT, verificado en vivo 2026-06-14). Criterios editoriales propios: (1) political_instability — theme-codes fuertes WB_2433/2432/2462/2465/PROTEST/GENERAL_GOVERNMENT/SLFID_DICTATORSHIP/EPU_POLICY + keywords crisis política (coup/uprising/election crisis); (2) commodities_energy — ENV_OIL/NATURALGAS/METALS/MINING/SOLAR/COAL/NUCLEAR + WB_507/2936/2937/1699 + prefijo ECON_* + keywords OPEC/LNG/crude; (3) critical_minerals — WB_895/1699+ENV_MINING/METALS + keywords rare earth/lithium/cobalt/nickel/neodymium/graphite/manganese/critical minerals/battery metals (sección keyword-dependiente, GAP-3 documentado); (4) semis_ai_tech — SOLO keyword/entity (sin theme-codes GKG propios) semiconductor/chip/fab/GPU/AI model/export control/chip ban + entityHints TSMC/ASML/Nvidia/Intel/Samsung/SK Hynix/Micron/Qualcomm/SMIC/Huawei; (5) digital_infra_cyber — keyword/entity cyberattack/ransomware/DDoS/data center/submarine cable + entityHints CISA/NSA/Cloudflare/CrowdStrike; (6) trade_sanctions — theme-codes WB_698/WB_439/ECON_*/UNGP_SANCTIONS/EPU_TRADE + keywords sanctions/tariff/embargo/OFAC + entityHints OFAC/WTO/BIS/G7/G20. classify(): match exacto o prefijo para themes (ECON_*, ENV_*), case-insensitive sobre title+themes-join para keywords, match parcial sobre organizations/persons para entityHints; precedencia theme>keyword>entity; dedup sección con el matchedBy de mayor precedencia. Tests cubren 5 acceptance de T-16 + precedencia + dedup + edge cases (null/vacíos/prefijo). NO toca barrel. NOTA calibración: mapa de partida editorial; refinar tras 1ª semana con datos reales (OQ-4). Self-Report: real.

## [2026-06-15] [PM] [DONE] — **Wave B (radar) CERRADA y VERIFICADA.** T-17 (conector GKG + util ZIP compartida) estaba a medias de una sesión previa (código escrito, sin cerrar). Estado hallado: `geo/zip.ts` (extractZipFirstEntry zero-dep) ✓, `geo/gkg.ts` (27-col TAB, classify, geo best-effort tipo3/4, PAGE_TITLE, V2Tone, single-flight+serve-stale) ✓ type-clean, `gdelt.ts` refactorizado para importar `./zip.js` ✓. **FALTABAN** (cerrados por el PM): (1) **regresión gdelt.test.ts** — importaba `extractZipFirstEntry` de `./gdelt.js` que ya no lo exporta (movido a zip.ts) → `SyntaxError`; FIX: split del import a `./zip.js`. (2) **`gkg.test.ts` no existía** → escrito (fixture 27-col TAB + ZIP deflate en memoria + stub fetch lastupdate→.gkg.csv.zip): cubre vacío-gracioso/500/304 sin lanzar, parseLastupdateGkg, parseGkgCsvRows→SignalRow (theme/keyword/entity vía classify, geo, tone, PAGE_TITLE, raw_json+matchedBy), descarte !=27 cols, descarte 0-secciones (D-203), end-to-end ZIP→SignalRow. (3) **barrel** `connectors/index.ts` cablea `fetchGkg` (L-4). **Consolidado PM: connectors tsc EXIT 0; 48/48 gkg+gdelt; 198/198 suite connectors completa; store+connectors dist reconstruidos (L-2, verificado: dist exporta fetchGkg + upsertSignals/getSignals/getSignalTrend/SignalRow); global `pnpm -w exec tsc --noEmit` EXIT 0.** Siguiente: Ronda 1 paralela = T-18 (scheduler job gkg→medium) ‖ T-19 (server /api/signals + trend) — files disjuntos, ambas deps (T-15/T-17) satisfechas; T-19 solo depende de T-15. Luego Ronda 2 = T-20 (web RadarPanel, consume /api/signals).

## [2026-06-15] [PM] [DONE] — **Ronda 1 paralela (T-18 ‖ T-19) CERRADA y VERIFICADA.** Dos backend-architect en paralelo, files disjuntos (scheduler vs server.ts), self-verify package-scoped; el PM corrió el tsc GLOBAL consolidado al cerrar (L-3, sin carrera porque ambos ya habían terminado). **T-18 scheduler**: `SchedulerDeps` gana `fetchGkg: ()=>Promise<ConnectorResult<SignalRow>>` + `upsertSignals`; `ConnectorDeps` añade 'fetchGkg'; `REAL_STORE_AI_DEPS` añade upsertSignals; `loadDefaultConnectors` añade fetchGkg; `defaultJobs` AÑADE job `gkg` tier medium (fetchGkg→upsertSignals si data.length>0) tras gdelt, SIN tocar otros jobs ni la firma. Verificado por el PM: scheduler tsc EXIT 0, **27/27 tests**, job gkg presente. (El agente truncó el report mid-frase por límite de turnos — L-6 — pero los ficheros quedaron en disco correctos; el PM remató el verify.) **T-19 server**: `GET /api/signals` (filtros section/since/limit/minToneMag; section inválida→400) + `GET /api/signals/trend?section=` (section requerida→400 si falta/inválida) — colocadas con `/api/signals/trend` ANTES de `/api/signals` (pathname más específico), `VALID_SECTIONS: Set<Section>`, solo-lectura, pipeline middleware intacto, wire camelCase L-1 (JSON.stringify directo). **37/37 tests** (26 previos health/markets/briefing/events + 11 nuevos T-19). **CONSOLIDADO PM: scheduler dist rebuilt (L-2), global `pnpm -w exec tsc --noEmit` EXIT 0, SUITE COMPLETA 338/338 / 0 fail** (store 49 + connectors 198 + scheduler 27 + core-ai 27 + server 37). Siguiente: Ronda 2 = T-20 (web RadarPanel + capas signals + map-tie, consume /api/signals) — EN CURSO (frontend-dev). Tras T-20: barrel ya tiene fetchGkg; gate final = global tsc + suite + smoke EN VIVO (curl /api/signals + trend) + browser E2E (RadarPanel + map-tie + responsive) + /verify goal-backward.

## [2026-06-15] [PM] [DONE] — **Ronda 2 (T-20 web) + GATE FINAL = RADAR GEOECONÓMICO (Fase 2 rebanada 2) CERRADO Y VERIFIED EN TODOS LOS NIVELES.** **T-20 web** (frontend-dev, truncó mid-verify por turnos L-6 → PM remató el verify): `client.ts` gana `RawSignalRow` camelCase (L-1, comentario BUG-1 explícito) + `adaptSignalRow` + `getSignals`/`getSignalTrend`; `layers.config.ts` gana `SIGNAL_LAYERS` (1 capa/sección, source 'signals', political_instability EXCLUIDA D-207, reusa events geo); `MapView.tsx` `signalsToGeoJSON` con **W-3 fix** (1 feature por señal×sección, `section` ESCALAR — evita 2º BUG-1); `RadarPanel.tsx` nuevo (6 secciones accordion, headlines rankeadas + trend canvas + entidades + estados loading/empty/error + atribución GDELT); `App.tsx` 3ª tab Radar + `activeSection` map-tie. **web tsc + build EXIT 0** (39 módulos). **GATE COMPLETO (PM, goal-backward G-1..G-7 + L-5):** global `pnpm -w exec tsc --noEmit` EXIT 0; **suite 338/338** (store 49 + connectors 198 + scheduler 27 + core-ai 27 + server 37); **SMOKE EN VIVO** (server real :8787 + DB poblada con fetch GKG real): **471 signals** clasificados (political_instability 405, commodities_energy 224, trade_sanctions 222, critical_minerals 39, semis_ai_tech 4, digital_infra_cyber 7), geo 90% (426/471); `/api/signals` wire **camelCase** confirmado (`signalId/tone/themes/sections:[{section,matchedBy}]` — clase BUG-1 limpia), filtro `?section=commodities_energy`→224, `/api/signals/trend?section=political_instability`→1 bucket vol 405 avgTone −1.52, `?section=nope`→400, trend sin section→400; **BROWSER E2E (Playwright, `radar-e2e.mjs`)**: desktop 6 secciones + **10 headlines render, badHeadlines=0** (parser cliente camelCase OK), **map-tie activo** (.radar-panel__section.active al seleccionar), atribución visible, responsive 375 sin overflow + drawer, **0 errores consola, 0 red, VERDICT PASS**. Screenshots `plans/screenshots/radar-{desktop-1200,mobile-375}.png`. **LECCIÓN reforzada (L-5):** el browser E2E confirmó que el parser cliente renderiza datos GKG reales (la clase exacta de BUG-1) — 338 tests + tsc + smoke-curl ven el wire, el E2E ve el render. **FOLLOW-UPS HONESTOS (no bloquean):** (1) **gkg timeout 8s en boot** — el job gkg dio timeout al arrancar (6 jobs concurrentes saturan), PERO la red real descarga el zip 3.08MB en **224ms** (medido) → es contención de boot, no límite duro; el conector degradó gracioso (vacío, sin crash); puebla en el ciclo 15min siguiente. Posible mitigación futura: subir TIMEOUT_MS del gkg o escalonar el boot. (2) **calibración clasificador** semis_ai_tech (4) / digital_infra_cyber (7) bajos = secciones keyword-dependientes (GAP-3); refinar tras 1ª semana con datos reales (OQ-4, intel-analyst). **PENDIENTE usuario:** commit (aprobación humana). `radar-e2e.mjs` queda en repo (convención, como `events-e2e.mjs`).

## [2026-06-15] [SYSTEM-ARCHITECT + PM] [DONE] — **Fase 2 rebanada 3 = CII SCORING. Design-doc REFRESCADO** `docs/design/2026-06-15-cii-scoring.md` (registrado por el PM; el architect no escribe este canal). El usuario eligió CII+convergencia. El doc viejo (2026-06-13) quedó OBSOLETO: su premisa "data-starved, solo Information con señal" murió al construir events+signals. **Refresco (D-200..213, 7 NG):** componentes {information,unrest,conflict,security} → **{conflict, economic, political, social}** con fuente REAL keyless: conflict←events category=conflict (severity+Goldstein), social←events protest+GKG political_instability (0.6/0.4)+boosts EQ/fire, economic←GKG commodities/trade/minerals×AvgTone, political←GKG political_instability+info-temp. EVENT_BLEND propio {0.25/0.30/0.20/0.25}, COMPOSITE {0.4/0.6}, FLOORS por componente, decay exp vida-media 30d, boosts EQ/fire vivos (USGS/EONET en events). Tier daily→**medium**. Migración 002_cii→**004_cii.sql**. **R-2/NG-5 (sesgo país-fuente, reverse-geocode) MUEREN**. Mantiene clean-room no-AGPL, persiste-antes-de-servir, config-array, sin LLM nuevo, N-componentes signalPresent+renorm, cii_snapshots+dynamicScore, /api/cii, briefing. Convergencia cross-domain SIGUE Non-Goal (NG-1).

## [2026-06-15] [PM] [DONE] — **HALLAZGO CRÍTICO (verificar-no-asumir) + OQs ratificadas + plan escrito.** Antes de planificar, el PM verificó `data/world.db`. **OQ-2 (clave país) — R-2 MATERIALIZADO:** `EventRow.country` HETEROGÉNEO — GDELT=**FIPS 10-4** (115 claves 2 letras: JA=Japón, MX=México, CI=Chile), USGS=**nombres** (Japan, Mexico, Chile). Usar la clave tal cual (recomendación D-203 del architect) PARTIRÍA cada país en 2 filas CII → boosts USGS nunca se sumarían al conflict GDELT → **índice roto** (clase GDELT-GEO-murió). **Ratificación PM:** el motor normaliza vía `normalizeCountryKey(raw,source)`→nombre canónico (FIPS→nombre para GDELT; identidad USGS/EONET; clave=nombre, encaja con country-centroids.ts → resuelve también OQ-6). 6 OQs ratificadas. **Plan `plans/2026-06-15-cii-scoring.md`** (7 tareas T-21..T-27, 3 rondas: A=21store‖22config · B=23motor‖25server · C=24job‖26web‖27briefing; T-25 solo dep T-21 → paralelo con motor). L-1..L-6 + **L-7 nueva (verificar clave país antes de agrupar)**. Nuevo paquete @www/core-cii (PM cabléa workspace+refs+dist antes de Ronda B). Convergencia + conectores keyed = Non-Goal. Gate `/check-plan` EN CURSO → aprobación usuario → implementar.

## [2026-06-15] [PLAN-CHECKER] [DONE] — Plan `cii-scoring` (Fase 2 rebanada 3): **PASS** (0 issues bloqueantes, 5 warnings no-bloqueantes). 5 dimensiones limpias (cobertura G-1..11 + 6 OQ + setup paquete; completitud verify<60s package-scoped; grafo acíclico con disjunción real por ronda A/B/C, T-25 solo dep T-21 no prematuro, core/cii/index.ts en rondas distintas; scope sin frases de erosión, convergencia NO es tarea; riesgos con dueño) + fidelidad bloqueadas D-001..005 íntegras. **OQ-2 (re-ratificación a NORMALIZAR) COHERENTE sin hueco**: anticipada por D-203, R-2 materializado (smoke), cableada T-22+T-23+test-L7+smoke-vivo, country-centroids keyed por nombre encaja. Warnings: (1) 6 áreas → sostener rebuild-dist L-2 entre rondas; (2) ~24 ficheros (scaffold paquete nuevo); (3) cobertura FIPS_TO_NAME → log claves no mapeadas; (4) server.ts alto-conflicto serializado; (5) L-1 camelCase repetible → curl+E2E al cierre. Informe `plans/check-plan-cii-scoring-2026-06-15.md`. Registrado por el PM (plan-checker read-only). Siguiente: aprobación del usuario → implementar ronda a ronda.

## [2026-06-15] [PM] [DONE] — **Ronda A (CII) COMPLETA y VERIFICADA.** Usuario aprobó arrancar. T-21 (store, backend-architect) ‖ T-22 (core-cii config, intel-analyst) en paralelo, paquetes disjuntos. **T-21**: `migrations/004_cii.sql` (cii_snapshots + ix_cii_country_time, W-2 evitado: test assertea sqlite_master), `CiiSnapshotRow` camelCase, `insertCiiSnapshots`(append, no upsert)/`getLatestCii`(1 fila/país, patrón getLatestMarkets)/`getCiiTrend`(ASC)/`getPriorCii`(<=aroundMs DESC LIMIT 1), `purgeAndDownsample` extendido a cii_snapshots. **62/62 tests**. **T-22**: paquete NUEVO `@www/core-cii` (espejo @www/core-ai) + `blend.config.ts` (EVENT_WEIGHTS {0.25/0.30/0.20/0.25} suma 1, FLOOR_FACTORS, COMPOSITE {0.4/0.6}, decayWeight vida-media 30d, BOOST, ECONOMIC_SECTIONS, SOCIAL_MIX; tipos CiiComponentKey/Section locales) + `coefficients.ts` (COUNTRY_COEFFS 64 países editoriales + DEFAULT_COEFF + COMPONENT_REGISTRY) + `country-key.ts` (**normalizeCountryKey FIPS→nombre, OQ-2/L-7**: trampas cubiertas CH=China/AS=Australia/SZ=Switzerland/UK=United Kingdom/JA=Japan…). **46/46 tests** (invariante suma=1, decay(0)=1/decay(30d)=0.5, JA≡Japan). **CONSOLIDADO PM (L-4 wiring del paquete nuevo): añadí dep `@www/store` a core-cii/package.json + `pnpm install` (7 proyectos, @www/store linkado en core-cii) + store dist rebuilt (CiiSnapshotRow+API en .d.ts) + core-cii dist built + global `pnpm -w exec tsc --noEmit` EXIT 0.** Siguiente: Ronda B = T-23 motor ‖ T-25 server (EN CURSO).

## [2026-06-15] [PM] [DONE] — **Rondas B y C (CII) COMPLETAS y VERIFICADAS.** **Ronda B** (T-23 motor intel-analyst ‖ T-25 server backend-architect, disjuntos): **T-23** `score.ts` (computeConflictComponent events:conflict severity+Goldstein; computeSocialComponent protest+GKG political_instability mix 0.6/0.4 + boosts EQ/fire; computeEconomicComponent GKG commodities/trade/minerals×tone; computePoliticalComponent; computeCii renorm sobre presentes + composite; computeDynamic deadband; **computeAllCountries re-agrupa con normalizeCountryKey L-7**) 86/86. **T-25** server `/api/cii` (getLatestCii + adjunta lat/lon de COUNTRY_CENTROIDS) + `/api/cii/:country` (getCiiTrend) solo-lectura camelCase 48/48. PM: core-cii dist rebuilt, global tsc EXIT 0, suite 452/452. **Ronda C** (T-24 scheduler ‖ T-26 web ‖ T-27 briefing): **T-24** job `cii` tier medium (computeAllCountries→computeDynamic vs getPriorCii→insertCiiSnapshots); 2 tests viejos (7-jobs/orden) actualizados a 8+cii por el PM (agente truncó L-6) 27/27. **T-26** web (RawCiiRow camelCase + adaptCiiRow + getCii/getCiiTrend; CII_LAYER circle por banda composite; MapView ciiToGeoJSON + flyTo map-tie; RiskPanel nuevo; **App.tsx 4ª tab Risk + activeCountry — el agente truncó ANTES de cablear la tab/render/state en App, el PM lo remató**; fix tsc dominant-reduce) web tsc+build EXIT 0. **T-27** briefing (intel-analyst con Bash DENEGADO se detuvo sin escribir → **lo hizo el PM**): `buildRiskContext` (top-8 países por composite + dynamic/trend + componente dominante de componentsJson; '' si vacío) + serializeContext gana el bloque desde getLatestCii (D-005 sin proveedor nuevo) 31/31. PM: core-ai+scheduler dist rebuilt, global tsc EXIT 0, suite 452/452.

## [2026-06-15] [PM] [DONE] — **GATE FINAL = CII SCORING (Fase 2 rebanada 3) CERRADO Y VERIFIED. + HALLAZGO del smoke (verde≠funciona, L-5).** **SMOKE EN VIVO (server real :8787, job cii medium en boot)**: el primer boot persistió solo **58** CII snapshots PERO el log mostró **cientos de `[core-cii/country-key] FIPS no mapeado`** — la tabla `FIPS_TO_NAME` de T-22 cubría solo ~64 países; **~50 códigos FIPS que GDELT produce en vivo faltaban → esos países se DESCARTABAN del índice, incluidos los HOTSPOTS de conflicto** (SY=Siria, AF=Afganistán, LE=Líbano, SO=Somalia, SU=Sudán, CG=RDC, CE=Sri Lanka, RP=Filipinas, KZ=Kazajistán…). Para un índice de INESTABILIDAD descartar los países más inestables es inaceptable (= la warning #3 del plan-checker materializada). **FIX (PM)**: expandí `FIPS_TO_NAME` con los ~50 códigos (vía tabla FIPS 10-4 autoritativa, WebFetch) + **corregí un BUG** (`TC:'United Arab Emirates'` — FIPS TC=Turks&Caicos; UAE es `AE`) + trampas verificadas (CD=Chad no Trinidad, SG=Senegal no Singapore, BN=Benin no Brunei, ES=El Salvador, WA=Namibia) + 2 tests de regresión. COUNTRY_COEFFS sin tocar (eventScore peso 0.6 hace que conflicto puntúe alto aunque baseline=DEFAULT; calibración=NG-7). **RE-SMOKE**: **109 CII snapshots** (antes 58, +51), hotspots presentes (Syria/Afghanistan/Lebanon/Somalia/Sudan), **Japan=1 fila (OQ-2 ok, 0 fugas FIPS de 2 letras)**, `/api/cii` wire camelCase + lat/lon (62/109 con centroide → mapa; 47 solo-panel), dynamicScore=0/stable (serie nueva R-5). **GATE COMPLETO**: global tsc EXIT 0 + **suite 454/454** (store 62 + connectors 198 + scheduler 27 + core-ai 31 + core-cii 88 + server 48); goal-backward G-1..G-11 + OQ-2 + D-001..005 en código real; **BROWSER E2E (Playwright `cii-e2e.mjs`)**: desktop 6 tabs, Risk tab → **109 países render badNames=0** (parser cliente camelCase OK, incl Kosovo FIPS KV nuevo), **map-tie activo** (.risk-panel__row.active al click → MapView flyTo), atribución, responsive 375 sin overflow + drawer, **0 errores consola/red, VERDICT PASS**. Screenshots `plans/screenshots/cii-{desktop-1200,mobile-375}.png`. **LECCIÓN L-5 reforzada (3ª vez)**: el smoke en vivo cazó un hueco de cobertura (FIPS) que 452 tests + tsc + /check-plan NO vieron (los tests usan fixtures con FIPS conocidos; solo el GDELT real expone los 115 códigos). **FOLLOW-UPS no-bloqueantes**: (1) ~134 warnings FIPS restantes = territorios diminutos/no-países (OC/OS/Antártida/Guernsey/Nauru) → drop aceptable; (2) baselineRisk editorial por país sin calibrar (NG-7, conflicto se sostiene por eventScore); (3) dynamicScore neutro hasta ≥2 snapshots ~24h (R-5). **PENDIENTE usuario**: commit (aprobación humana). `cii-e2e.mjs` queda en repo (convención).

## [2026-06-15] [PM] [DONE] — **Rebanada 3 (CII) COMMIT + PUSH.** `c9e4cdf` en `main` (40 ficheros, +4916/−48), pusheado a origin (`b4046ee..c9e4cdf`). Árbol limpio.

## [2026-06-15] [SYSTEM-ARCHITECT + PM] [DONE] — **Fase 2 rebanada 4 = CONVERGENCIA: design-doc COMPLETO + VERIFICADO + 2 GAPs verificados en vivo + 2 decisiones de usuario.** El PM re-dispatchó al system-architect con Q1=(A) ratificado → `docs/design/2026-06-15-convergence.md` (10 secciones, D-300..312, 7 NG, status draft). El architect resolvió Q2-5 y cazó honestamente la contradicción **OQ-H**: `political×economic` mapean ambos a familia `signals` → NO son par independiente bajo anti-doble-conteo (D-306); el set realmente detectable = `events×signals` + `cualquiera×markets`. **VERIFICACIÓN-NO-ASUMIR del PM (2 gaps, código + DB viva):** **GAP-1 (markets)** — leído `market_snapshots {symbol,price,change_pct,captured_at}`+`market_daily` OHLC; **NO existe `regimeDelta`/indicador de régimen** (la D-303 original lo asumía); derivable de change_pct/OHLC. **GAP-4 (overlap)** — probe contra `data/world.db` último `cii_snapshots`: **109 países, 63 con solapamiento events-family×signals-family** (signalPresent en conflict/social Y en economic/political); 45 solo-events, 1 solo-signals, 0 ninguno → **MVP events×signals VIABLE, no vacío**. **2 decisiones del usuario:** (1) **markets ENTRA al MVP** (no se difiere) → el architect enmendó D-303/D-305/{conv.market.stress} con receta clean-room concreta `magnitude=max(riskOff, vol)` (compuesto risk-off ponderado sobre change_pct de los 7 símbolos + proxy volatilidad OHLC; refs `MARKET_REF`/`RISKOFF_REF=3.0`/`VOL_REF=0.04` en `convergence.config.ts`) → **GAP-1 cerrado** (ya no asume campo inexistente). (2) **superficie = SOLO briefing + persistencia** (D-311/NG-4, sin /api/convergence ni mapa en esta rebanada). **PM RATIFICA las 10 OQs** (A,B,C,C2,D,E,F,G,H,I) a las recomendaciones del architect (dynamicScore G SÍ entra, 0 en 1ª ventana). Doc re-leído por el PM tras la enmienda: coherente (nota menor: G3/G4 Goals con fraseo viejo, las Decisions mandan). Quedan abiertos solo GAP-2/3 = calibración diferida (no bloqueantes). Siguiente: ADR-012 → plan `plans/2026-06-15-convergence.md` → /check-plan → implementar.

## [2026-06-15] [PM] [DONE] — **ADR-012 + plan convergence escrito + `/check-plan` = PASS (tras fix de 1 bloqueante).** ADR-012 en DECISIONS.md (alcance rebanada 4: markets-in con receta de estrés clean-room, superficie briefing-only, anti-doble-conteo por dataFamily, familias MVP events×signals + cualquiera×markets). Plan `plans/2026-06-15-convergence.md` (5 tareas T-28..T-32, 3 rondas A/B/C, paquete NUEVO @www/core-signals, SIN UI). **plan-checker: ISSUES_FOUND → 1 bloqueante (ISSUE-1) cazado ANTES de codear:** el scheduler corre los jobs de un tier en PARALELO (`Promise.all` boot :114 + `setInterval` por job :107; test :364 "re-orders by tier, not position") → un job `convergence` hermano "tras cii" NO leería los cii_snapshots de la corrida actual (1er boot: getLatestCii vacío → convergence_signals sin poblar). **FIX (PM, opción 1 del checker): encadenar detectAllConvergence+insertConvergenceSignals DENTRO del `run()` del job cii, tras insertCiiSnapshots → orden por construcción** (C-4 corrección de realidad + test de orden real). Warning-1 (T-32 globs→ficheros concretos briefing.ts/index.ts/ai.test.ts) resuelto. **Re-check plan-checker = PASS** (2 warnings no-bloqueantes: 4 áreas, calibración GAP-2 diferida). Verificaciones de código del PM en el camino: GAP-1 (no existe regimeDelta), GAP-4 (63/109 overlap events×signals), C-1 (market_daily solo lo puebla purgeAndDownsample→vacío en ventana 72h; estrés markets desde market_snapshots), MarketSnapshot es snake_case legacy. Pendiente: aprobación del usuario → implementar Ronda A (T-28 store ‖ T-29 core-signals puro).

## [2026-06-15] [SYSTEM-ARCHITECT + PM] [SUPERSEDED] — **(entrada previa, cerrada por la de arriba) Fase 2 rebanada 4 = MOTOR DE CONVERGENCIA cross-domain (el último diferencial, era NG-1). RPI Research INICIADA, design-doc PENDIENTE (cerramos sesión).** El PM dispatchó system-architect para diseñar `docs/design/2026-06-15-convergence.md` (consume las 4 capas ya construidas como observaciones: CII components por país + events + signals GKG + markets; metodología skill cii-scoring ≥2-fuentes/72h/magnitud≥0.5; no-AGPL; migración 005; /api/convergence + panel + resaltado mapa; MVP tractable, familias no-computables con datos actuales = DIFERIDAS). El architect corrió exploración + paró en interrogación **Q1 (arquitectura): ¿qué son "fuentes independientes" si el CII ya agrega events+signals?** (anti-doble-conteo). 3 opciones: (A) CII-componentes como capa de observación canónica + markets exógeno, independencia=familias-de-dato disjuntas + ≥1 fuente no-CII; (B) events+signals+markets crudos ignorando el CII; (C) híbrido con grafo de independencia. **PM RATIFICA (A)** (usa el CII como lo que es — normalización canónica por-dimensión por-país; evita doble-conteo por construcción; markets = única fuente verdaderamente exógena). **PRÓXIMA SESIÓN**: re-dispatch system-architect con Q1=(A) ratificado → completar interrogación (Q2-5, esperadas sobre: granularidad temporal de observaciones, mapeo magnitud por fuente, entidad para markets-global, persistencia/dynamic, alcance MVP de familias) → design-doc → /check-plan → implementar. NO se escribió código de producto. Handoff en `.claude/handoffs/`.

## [2026-06-15T23:58:00Z] [INTEL-ANALYST] [DONE] — **T-29 @www/core-signals COMPLETADO. 52/52 tests verdes. tsc limpio.** Paquete NUEVO `packages/core/signals` creado clean-room (ADR-002/D-001, no-AGPL). Ficheros: `package.json` (espejo core-cii, deps @www/store+@www/core-cii declaradas para PM/L-4) + `tsconfig.json` + `src/convergence.config.ts` (parámetros: MIN_SOURCES=2, MIN_MAGNITUDE=0.5, WINDOW_MS=72h, HALF_LIFE_72H=36h, METHODOLOGY_VERSION='conv-core-1'; FAMILY_OF D-306; MARKET_REF 7 símbolos pesos-suman-1.0 editorial re-derivado dir±1; RISKOFF_REF=3.0/VOL_REF=2.0) + `src/detect.ts` (tipos locales DataFamily/ConvergenceDimension/ConvergenceObservation/ConvergenceSignal + detectConvergence PURA: filtro ventana+umbral+signalPresent-D304, anti-doble-conteo por familia-D306, markets transversal D305 solo países con economic, strength time-decayed D307 w=0.5^(age/HALF_LIFE), dynamicScore=0/firstDetectedAt=nowMs iniciales para IO) + `src/magnitude.ts` (clamp01, ciiMagnitude score/100, marketRiskOff compuesto-risk-off-ponderado-direccional, marketVol dispersión max-min, marketStress=max(riskOff,vol)) + `src/index.ts` (barrel). Tests: `test/magnitude.test.ts` (MARKET_REF invariante pesos=1.0, clamp01, ciiMagnitude, marketRiskOff direccional stress/calm/unknown/extremos, marketVol, marketStress) + `test/detect.test.ts` (R1 anti-doble-conteo, umbral MIN_MAGNITUDE, D-304 signalPresent, ventana WINDOW_MS, D-305 markets transversal, D-307 strength decay numérico exacto, valores iniciales, múltiples países, invariantes config). Verify: `pnpm --filter @www/core-signals exec tsc --noEmit` EXIT 0 + `node --import tsx --test .../*.ts` → **52/52 pass, 0 fail** en <60s. Autofix Regla-1 aplicado: `await import(...)` en callback `it()` sincrono → declarado `async` (1 intento, resuelto). Self-Report: real.

## [2026-06-15] [PM] [DONE] — **Ronda A (convergencia) COMPLETA y VERIFICADA (verify propio del PM).** T-28 (store, backend-architect) ‖ T-29 (core-signals puro, intel-analyst), paquetes disjuntos. **T-28**: `005_convergence.sql` (convergence_signals + ix_conv_country_time, W-2 evitado) + `ConvergenceSignalRow` camelCase + insertConvergenceSignals(append)/getLatestConvergence(último por country+familiesJson)/getPriorConvergence(<=aroundMs, mismo familyset) + purgeAndDownsample Step-7 (cii_snapshots intactos). **OJO L-6: el backend-architect tuvo Bash DENEGADO → fabricó/estimó su verify; el PM lo corrió de verdad → tsc limpio + 74/74.** **T-29**: paquete NUEVO @www/core-signals PURO (tipos locales, sin import store) + convergence.config.ts + magnitude.ts + detect.ts (detectConvergence pura, anti-doble-conteo D-306, markets transversal D-305, strength decay D-307) + 52 tests (incl R1). **CONSOLIDADO PM (L-2/L-4): pnpm install 8 proyectos (@www/core-signals linkado + symlinks @www/store/@www/core-cii confirmados para T-30), store dist rebuilt, core-signals dist built (index.d.ts), global tsc EXIT 0, suite store+core-signals 126/126.** Siguiente: Ronda B = T-30 observe (IO) ‖ T-32 briefing (EN CURSO).

## [2026-06-16T00:00:00Z] [INTEL-ANALYST] [DONE] — **T-32 @www/core-ai enriquecimiento briefing convergencia COMPLETADO. 45/45 tests verdes. tsc limpio.** Ficheros: `packages/core/ai/src/briefing.ts` + `packages/core/ai/src/index.ts` + `packages/core/ai/test/ai.test.ts`. Cambios: (1) `buildConvergenceContext(latest: ConvergenceSignalRow[]): string` — top-8 por strength, formato `<país> (<familias+separadas>, strength <XX.YY>, <↑/↓/→>)`; '' si vacío; flecha del dynamicScore; familiesJson con fallback gracioso ante JSON inválido; exported en index.ts. (2) `serializeContext` gana 4º parámetro `convergence: ConvergenceSignalRow[] = []`; inserta bloque de convergencia si no vacío. (3) `generateDailyBriefing` lee `getLatestConvergence()` del store (patrón idéntico a getLatestCii) y lo pasa como 4º arg. GUARDRAILS: sin proveedor LLM nuevo (ADR-009 intacto), caché D-106 intacta, sin import circular, no toca router. Tests: 2 nuevas suites (Suite 7 `buildConvergenceContext` 10 tests + Suite 8 `serializeContext() con convergencia` 4 tests), cubren criterio de disparo (incluye/excluye), flechas {↑/↓/→}, fallback JSON inválido, límite top-8, integración con los 3 bloques previos. Verify: `pnpm --filter @www/core-ai exec tsc --noEmit` EXIT 0 + `node --import tsx --test packages/core/ai/test/*.ts` → 45/45 pass 0 fail.

## [2026-06-16] [PM] [DONE] — **Rondas B+C (convergencia) COMPLETAS + GATE FINAL = MOTOR DE CONVERGENCIA (rebanada 4) VERIFIED. + HALLAZGO del smoke EN VIVO (L-5, 4ª vez) + CALIBRACIÓN.** **Ronda B**: T-30 observe.ts **lo escribió el PM** (el intel-analyst murió por error de API socket, 0 ficheros) — `detectAllConvergence`: arma obs desde getLatestCii (componentes signalPresent→familia) + markets transversal (getLatestMarkets/getMarketTrend, **MarketSnapshot es snake_case legacy** change_pct/captured_at, C-1) → detectConvergence → enriquece firstDetectedAt/dynamicScore vs getPriorConvergence → ConvergenceSignalRow[]; 6 tests (incl R1 end-to-end). T-32 briefing (intel-analyst, con Bash) 45/45. **Ronda C**: T-31 **lo escribió el PM** — encadenó detectAllConvergence+insertConvergenceSignals DENTRO del run() del job cii (tras insertCiiSnapshots, C-4/D-312, orden por construcción), SchedulerDeps+REAL_STORE_AI_DEPS ganan las 2 deps, +@www/core-signals a scheduler/package.json; **3 tests nuevos incl. el de ORDEN real** (insertCii→detectConv→insertConv vía mocks). CONSOLIDADO: global tsc EXIT 0, suite 541/541. **SMOKE EN VIVO #1 (L-5 cazó el problema)**: boot real encadenó cii→convergencia SIN crash PERO **0 señales** — diagnóstico PM: la familia **signals (economic/political) del CII corre en escala diminuta** (max score ~7.6 vs conflict 0..100) → con magnitud lineal score/100 NUNCA llega a 0.5 → el par primario events×signals es **estructuralmente imposible**. + markets change_pct null hoy. **CALIBRACIÓN (usuario eligió 'calibrar ahora')**: normalización de magnitud POR-DIMENSIÓN `DIMENSION_SCALE {conflict:80, social:50, economic:7.6, political:7.6}` (derivada de la distribución real 2026-06-16, NO toca el CII NG-6), `ciiMagnitude(score, dimension)`, methodology_version→**conv-core-2**. **RE-SMOKE EN VIVO = PASS**: boot → `cii→convergence: persisted 10 signals` mismo boot → **10 señales reales** (Iraq/Israel/Pakistan/Russia/Ukraine/Palestinian Territories/Poland/Kenya/Norway/South Africa), todas events×signals sourceCount=2, **anti-doble-conteo OK** (sourceCount==familias-distintas>=2), strength∈[0,1] (0.72-0.91), conv-core-2; **briefing diario corrió** (openai/gpt-5.4) tras poblar convergencia (boot-sequencing) → leyó las 10. Sin /api ni UI (NG-4). **FOLLOW-UPS no-bloqueantes**: (1) calibración fina DIMENSION_SCALE con ≥semanas de snapshots (GAP-2, intel-analyst); (2) markets change_pct null hoy (Yahoo no dio %) → markets no corrobora; (3) Norway algo dudoso (ruido GDELT QuadClass, follow-up histórico). **PENDIENTE usuario**: commit (aprobación humana) + proponer rebanada-superficie UI de convergencia (lo pidió el usuario).

## [2026-06-16] [PM] [DONE] — **Rebanada 4 COMMIT+PUSH `be40fa5` + Rebanada 5 (superficie UI convergencia) RPI: design-doc + ADR-013 + plan + /check-plan PASS → Ronda A lanzada.** Commit `be40fa5` (32 ficheros) pusheado a origin/main. **Rebanada 5** = ESPEJO estricto de la superficie CII: `docs/design/2026-06-16-convergence-surface.md` (D-400..409, 6 NG) — `GET /api/convergence` solo-lectura + capa **anillo** (distinto del círculo CII, coexisten; toggle independiente apagado) + ConvergencePanel (empty-state primera clase) + 5ª pestaña + map-tie, camelCase, **NO toca el motor (NG-1)**. **PM cerró 2 gaps**: GAP-2 ("Palestinian Territories" FALTA en country-centroids → T-33 lo añade; otros 9/10 vivos están) + GAP-1 (componentsJson=ConvergenceObservation[] → topDimension=obs de mayor magnitud). OQs: trend por-país DIFERIDO, anillo por strength, capa apagada. **ADR-013**. Plan `plans/2026-06-16-convergence-surface.md` (T-33 server+centroide ‖ T-34 web). **plan-checker = PASS** (0 bloqueantes, 3 warnings; verificó código real: server.ts:432, getLatestConvergence camelCase, MapView:192 itera spread, server.ts:43 importa centroides por path relativo→no rebuild dist). Ronda A EN CURSO: T-33 (backend-architect) ‖ T-34 (frontend-dev). Cierre = global tsc + suite + smoke EN VIVO (curl /api/convergence camelCase, 10 países lat/lon) + browser E2E (anillo + panel + map-tie + empty-state + responsive).

## [2026-06-16] [PM] [DONE] — **Ronda A (superficie convergencia) COMPLETA + GATE FINAL = SUPERFICIE UI DE CONVERGENCIA (rebanada 5) VERIFIED.** T-33 (backend-architect, con Bash) ‖ T-34 (frontend-dev). **T-33**: `GET /api/convergence` solo-lectura (espejo /api/cii, getLatestConvergence + lat/lon por centroide, NUNCA toca el motor — verificado: server.ts NO importa @www/core-signals) + **centroide "Palestinian Territories"** añadido (GAP-2). server.test 57/57. **T-34** (truncó mid-step-5/App.tsx L-6, PERO completó: web tsc+build EXIT 0): `client.ts` (RawConvergenceRow camelCase + adaptConvergenceRow + getConvergence + view-model ConvergenceCountry, topDimension de componentsJson GAP-1) + `CONVERGENCE_LAYERS` anillo (relleno transparente + stroke ámbar→rojo por strength, radio MAYOR que CII R-5, toggle independiente OFF por defecto D-403) + `convergenceToGeoJSON` (props escalares W-3) + `convergenceDataRef` (flyTo R-2) + `ConvergencePanel.tsx` (estados loading/empty-informativo/error D-408, orden por strength, map-tie) + 5ª pestaña. **CONSOLIDADO PM (verify propio, no relay)**: global tsc EXIT 0, suite **550/550** (+9 server); **smoke EN VIVO curl /api/convergence** = 11 señales, **0 keys snake_case** (camelCase end-to-end anti-BUG-1), Palestinian Territories lat/lon OK (GAP-2 fix), componentsJson=ConvergenceObservation[] (GAP-1); **browser E2E (qa-tester mcp_playwright, 19/19 PASS** + PM revisó screenshots): panel lista señales reales badNames=0, anillos renderizan + coexisten con círculos CII (R-5), map-tie flyTo, responsive 375/1200; `convergence-e2e.mjs` + 6 screenshots en repo. Verifier checks (PM): 0 stubs/TODO, ruta solo-lectura, config-array iterado (no addLayer imperativo), camelCase. **FOLLOW-UP no-bloqueante**: "Cuba" (y países que el motor produzca sin centroide) → panel-only (R-1, gracioso D-400; tabla COUNTRY_CENTROIDS incompleta, mismo límite que el CII). qa-report `plans/qa-report-convergence-2026-06-16.md`. **PENDIENTE usuario**: commit (aprobación humana).

## [2026-06-16] [PM] [DONE] — **CIERRE DE SESIÓN 2026-06-16. Rebanadas 4+5 + ponytail + deuda + OFAC, todo pusheado (árbol limpio en `a649be3`).** Tras rebanada 5: **commit+push** `be40fa5` (conv motor) + `16a1356` (conv UI). **ponytail integrado** `4dc4582` (RPI: brainstorming → híbrido [plugin global ~/.claude espejo caveman + AGENT-CONTRACTS §9 que propaga vía scope-payload.shared + 1 línea en 5 defs coders] → /check-plan PASS → implementado; el usuario instaló+recargó el plugin). **/ponytail-audit** cazó ConnectorResult 7 copias→1 → `8a17c10`. **Deuda convergencia** `5303ff1` (slice lean): COUNTRY_CENTROIDS +49 (0 panel-only, hotspots al mapa) + markets change_pct derivado de chartPreviousClose. **Educación DESCARTADA** por el usuario (YAGNI). **OFAC sanctions** `98a5ac2` (Finanzas, Approach B sin UI, RPI completo): conector keyless OpenSanctions + tabla `sanctions` (migr 006) + job slow + briefing; T-35..T-38 (rematé T-37 truncado); smoke vivo 190 países (Russia 5597/Iran 1918/NK 415); el smoke cazó+arregló 2 alias ISO (Türkiye→Turkey, HK SAR China→Hong Kong, L-5). `a649be3` = roadmap doc. **Suite 603/603, global tsc EXIT 0.** Memorias del harness actualizadas (MEMORY.md, world-wide-project-goal, world-wide-data-feeds-state, **feedback-ponytail-minimalism** nueva). Handoff: `.claude/handoffs/session-2026-06-16-convergence-ui-ponytail-ofac.md`. **Lo keyless mayor agotado** → siguiente = keyed (FRED/EIA, ACLED/UCDP — necesitan keys), UI-superficie de sanciones, o Fase 4 pulido. Sesión cerrada por el usuario.

## 2026-06-19 [PLAN-CHECKER] [DONE] — Plan slice-d-interactive-map-spanish: PASS (0 issues, 5 warnings).

## [2026-06-19] [PM] [DONE] — **Slice D (mapa interactivo + español) CERRADA+VERIFIED — Fase 5 IA-first COMPLETA (4/4).** RPI completo: brainstorming (3 decisiones del usuario: popup on-demand cacheado · todos los puntos clicables · UI español completa) → design-doc `docs/design/2026-06-18-slice-d-interactive-map-spanish.md` (D-900..908, ADR-018) → plan → **/check-plan PASS** (`plans/check-plan-slice-d-2026-06-18.md`, 0 bloqueantes, 5 warnings; los 3 accionables aplicados: test preflight OPTIONS, test web en `packages/web/test/` para entrar al glob, popup DOM por E2E sin jsdom). **6 tareas commiteadas**: (1) store migr 008 `translations` + get/put helpers; (2) `POST /api/translate` cache-first = **ÚNICA excepción a no-LLM-on-request (ADR-018/D-902)**, guard 1..500, CORS+POST, degrada `{translated:null}`; (3) client `translate()` + `i18n/countries.ts` `localizeCountry` (`Intl.DisplayNames` en→ISO2→es, reverse-map A-Z + alias); (4) MapView 1 click handler + `INTERACTIVE_LAYER_IDS` (derivado, no-heatmap) + `map/popup.ts` (`popupRows` puro + `buildPopupNode`, botón Traducir solo en texto libre); (5) UI 100% español (pestañas + 7 paneles + estados + localizeCountry en presentación); (6) verify. **GATE FINAL VERIFIED**: tsc paquetes+raíz EXIT 0, suite **349/0**, server **72/0** (incl. translate cache-hit/400/405/preflight), web build OK; **smoke EN VIVO gpt-5.4** (`/api/translate` "oil tanker seized near Strait of Hormuz…"→"petrolero incautado cerca del estrecho de Ormuz…" 1.5s, cache-hit 0.05s, empty→400); **browser E2E 13/13** (`slice-d-e2e.mjs`: pestañas ES, popup ES "Estrecho de Gibraltar"/"Terremoto", **botón Traducir EN VIVO** traduce un titular USGS "8 km NE of Bel Air North"→"8 km al noreste de Bel Air Nor…", 375px sin overflow). Hook DEV `window.__wwMap` para proyección coord→píxel en E2E (gated, sin efecto prod). **NO toca motor A/B/C ni migraciones 001-007 (NG-1).** Follow-up no-bloqueante: poblar `COUNTRY_ALIASES` con mismatches reales. **PENDIENTE usuario**: commit ya hecho localmente (6 commits), `git push` cuando quiera.

## [2026-06-19] [VERIFIER] [VERIFIED] Slice D (mapa interactivo + espanol) — 24/24 condiciones OK. Gates: tsc EXIT 0, suite 349/0, server.test 72/0, vite build OK. Sin anti-patrones (catch /api/translate = D-907 intencional). Wiring completo. NO toca motor A/B/C. **Fase 5 IA-first COMPLETA (4/4).**

## [2026-06-19] [VERIFIER] [VERIFIED] UI Redesign Slice 1 (command-center reskin) — backend/store/motor intactos (diff HEAD~6 vacío), MapLibre sigue v4, tokens/glow(top-level radius)/popup-CSS/IntelPanel verificados, Slice D preservado. Gates: tsc EXIT 0, suite 351/0, server 72/0, web build OK. slice-d-e2e 11/0 + redesign-e2e 5/5. **Fase 6 Slice 1 CERRADA; Slice 2 = globo (v5).**

## [2026-06-19] [VERIFIER] [VERIFIED] UI Redesign Slice 2 (globo 3D) — 16/16 condiciones OK. NG-1 confirmado (diff Slice 2 = solo web/package.json + MapView.tsx + lock). setProjection/setSky/rAF todos dentro de style.load (ISSUE-2 resuelto). Cleanup cancela rAF+timer. Back-face cull en ambos E2E. Gates: tsc EXIT 0 (0 errores), suite 351/0, server 72/0, vite build EXIT 0 (14 s). ADR-020 registrado. Fase 6 COMPLETA (2/2).

## [2026-06-19] [PM] [DONE] — **Rediseño UI Slice 2 (globo 3D) CERRADO — Fase 6 COMPLETA (2/2).** MapLibre v4.7→v5.24 (+@types/geojson, breaking cazado por tsc) + globo `setProjection` en `style.load` + atmósfera `setSky` + rotación rAF (pausa en gesto/flyTo via isEasing, reanuda 4s). plan-checker cazó 2 issues (back-face cull E2E + orden rAF) → resueltos. slice-d-e2e 13/13 (Traducir en vivo sobre la esfera) + redesign-e2e 6/6 (projection=globe). NO toca backend/store/motor. PENDIENTE usuario: verifier (lanzado) + push.

## [2026-06-19] [VERIFIER] [VERIFIED] UI Redesign Slice 2 (globo 3D) — 16/16 condiciones OK. NG-1 confirmado (diff = solo web/package.json + MapView + lock). setProjection/setSky/rAF todos dentro de style.load (ISSUE-2 resuelto). Cleanup cancela rAF+timer. Back-face cull en ambos E2E. Gates: tsc EXIT 0, suite 351/0, server 72/0, build OK. **Fase 6 COMPLETA (2/2).**
