# QA Report — 2026-06-13

**Ejecutado:** 2026-06-13T20:09:18Z
**App:** http://localhost:5173 (Vite HMR, fix contraste cliente↔API aplicado)
**API:** http://localhost:8787
**Self-Report:** `real` — evidencia de ejecución Playwright sincrónica incluida abajo.

---

## Resumen Ejecutivo

**PASS** — La UI renderiza correctamente tras el fix de contraste. Los 7 instrumentos financieros aparecen con precios reales. El mapa MapLibre carga. Cero errores de consola JS ni errores de red (4xx/5xx). Solo warnings WebGL de driver GPU (nivel OS, no bug de app).

---

## Resultados por Flujo

| # | Flujo | Resultado | Notas |
|---|-------|-----------|-------|
| 1 | Carga de página (1200x800) | **PASS** | Título "World Intelligence Platform", sin pantalla blanca ni crash |
| 2 | Panel Finanzas — 7 instrumentos | **PASS** | BTC-USD $64,216 · ETH-USD $1,676 · SPY $741 · QQQ $721 · GLD $386 · EURUSD=X €1.16 · DX-Y.NYB $99 |
| 3 | Sparkline al clickar instrumento | **PASS** | 4 elementos canvas/svg detectados tras click |
| 4 | Mapa MapLibre (basemap OSM) | **PASS** | 2 canvas detectados (mapa + overlay) |
| 5 | Toggle GDELT visible en UI | **INFO** | Texto "gdelt" no aparece en el body visible — capa existe en config-array pero sin label expuesto en la UI (esperado: capa vacía, NG-2) |
| 6 | Responsive 375x812 (móvil) | **PASS** | Sin overflow (scrollWidth ≤ innerWidth), screenshot guardado |

---

## Errores de Consola

```
[WARNING] GL Driver Message (OpenGL, Performance, GL_CLOSE_PATH_NV, High): GPU stall due to ReadPixels
[WARNING] (x3, luego: "this message will no longer repeat")
```

**Clasificación:** INFO/WebGL-driver — warnings del driver GPU de Chromium headless capturando screenshots con ReadPixels. NO son errores de la aplicación. No aparecen en producción con GPU real.

**Errores JS:** ninguno.

---

## Errores de Red

Ninguno (0 respuestas 4xx/5xx).

---

## Tabla de Bugs

| ID | Severidad | Area | Descripcion | Repro |
|----|-----------|------|-------------|-------|
| — | — | — | Sin bugs detectados | — |

---

## Observaciones UX (INFO, no bugs)

| # | Area | Observacion |
|---|------|-------------|
| 1 | Finanzas | `+0.00%` en todos los instrumentos — el campo variación porcentual no calcula diferencial (store solo guarda snapshot puntual, sin historia previa en esta sesión). Esperado en MVP, no bug. |
| 2 | GDELT | No hay toggle visible con label "gdelt" — la capa existe en config-array pero la UI no expone label de capa interactivo. Si el toggle está planeado para Fase 2, ok. |
| 3 | Briefing IA | No testeado directamente (estado "no disponible" esperado sin OpenAI key activa). |

---

## Screenshots

- Desktop 1200x800: `plans/screenshots/desktop-1200x800.png`
- Mobile 375x812: `plans/screenshots/mobile-375x812.png`

---

## Para PM Coordinator

- El fix contraste cliente↔API funciona. La UI consume `/api/markets` correctamente y renderiza los 7 instrumentos con precios reales.
- El gate E2E está en verde: sin crashes, sin errores de red, responsive OK.
- El único item pendiente relevante: el `+0.00%` sugiere que el diferencial requiere al menos 2 snapshots en la DB (segunda ejecución del scheduler o datos históricos). No es bug bloqueante.
- La plataforma está lista para que el PM declare Fase 1 integrada (pendiente aprobación humana de commit).
